
1 of 6 complete.
Home
HashiCorp Certified Terraform Associate 004 - Practice Exams
Terraform Associate 004 - Exam #1 - Results
Attempt 2

Question 1Skipped

You're invoking a module that creates a subnet. The root load balancer module requires that subnet’s ID. How should you expose the ID and pass it to the load balancer module?

modules/subnets:

    resource "aws_subnet" "bk" {
      vpc_id     = aws_vpc.bk.id
      cidr_block = var.subnet_cidr
     
      tags = {
        Name = "BK Subnet"
      }
    }

Correct answer

add an output block to the subnet module and pass the value for the load balancer module using module.subnets.subnet_id
Explanation

By adding an output block to the subnet module, you can expose the subnet ID as an output variable. This allows you to pass the subnet ID to the load balancer module using the module.subnets.subnet_id syntax, ensuring that the required information is accessible to the load balancer module.

references to resources that are created within a module cannot be used within other modules
Explanation
The statement that references to resources created within a module cannot be used within other modules is incorrect. It is possible to expose and pass resource IDs between modules by using output variables and module references. This allows for modular and reusable configurations in Terraform.

declare a local in the root module and set it to aws_subnet.subnets.id from the child.
Explanation

Declaring a local in the root module and setting it to aws_subnet.subnets.id from the child module may not be the most efficient way to expose and pass the subnet ID to the load balancer module. Using locals in this manner may introduce unnecessary complexity and may not align with best practices for module communication.

move the subnet and load balancer resource blocks into the main configuration file so they can easily be referenced
Explanation

Moving the subnet and load balancer resources into the main configuration file may make them easier to reference, but it does not address the specific requirement of exposing the subnet ID and passing it to the load balancer module. This approach may not provide a clean and modular solution to the problem at hand.
Overall explanation

Objective 5c - Use modules in configuration

Modules also have output values, which are defined within the module with the output keyword. You can access them by referring to module.<MODULE NAME>.<OUTPUT NAME>. Like input variables, module outputs are listed under the outputs tab in the Terraform registry.

Module outputs are usually either passed to other parts of your configuration, or defined as outputs in your root module.

https://learn.hashicorp.com/tutorials/terraform/module-use#define-root-output-values
Resources
Domain
Objective 5 - Terraform Modules
Question 2Skipped

What environment variable can be set to enable detailed logging for Terraform?

TF_DEBUG
Explanation

Terraform does not use a TF_DEBUG environment variable for logging. Detailed logs are enabled with TF_LOG.
Correct answer

TF_LOG
Explanation

The TF_LOG environment variable can be set to enable detailed logging for Terraform. It allows users to see more detailed information about the actions Terraform is taking, which can be helpful for troubleshooting and understanding the execution flow.

TF_TRACE
Explanation

Terraform does not define a TF_TRACE environment variable. Logging verbosity is controlled with TF_LOG (and optionally TF_LOG_PATH), not TF_TRACE.

TF_INFO
Explanation

Terraform does not use a TF_INFO environment variable for logging. Detailed logs are enabled with TF_LOG.
Overall explanation

Objective 7c - Describe when and how to use verbose logging

Terraform has detailed logs that can be enabled by setting the TF_LOG environment variable to any value. This will cause detailed logs to appear on stderr.

You can set TF_LOG to one of the log levels TRACE, DEBUG, INFO, WARN or ERROR to change the verbosity of the logs. TRACE is the most verbose and it is the default if TF_LOG is set to something other than a log level name.

https://developer.hashicorp.com/terraform/internals/debugging
Resources
Domain
Objective 7 - Maintain Infrastructure with Terraform
Question 3Skipped

Which feature of HCP Terraform enables you to publish and maintain a set of custom modules that can only be used within your organization?

the public Terraform Registry
Explanation
The public Terraform Registry is a repository of publicly available Terraform modules that can be accessed by anyone. It does not provide the level of privacy and control needed to restrict module usage to only within your organization.

a custom VCS integration to your repo
Explanation
While a custom VCS integration to your repo can help manage your Terraform code, it does not provide the specific feature of enabling you to publish and maintain custom modules that are restricted to use within your organization.

HCP Terraform agents
Explanation
HCP Terraform agents are responsible for executing Terraform runs in the HCP environment and do not have the functionality to enable the publishing and maintenance of custom modules for internal use within your organization.
Correct answer

HCP Terraform private registry
Explanation
The HCP Terraform private registry allows you to publish and maintain a set of custom modules that are only accessible within your organization. This ensures that your organization's specific modules are securely stored and can only be used by authorized users within your organization.
Overall explanation

Objective 8b - Describe HCP Terraform collaboration and governance features

You can use modules from a private registry, like the one provided by HCP Terraform. Private registry modules use source strings in the form of a specific format. This format is the same as the public registry's but adds a hostname prefix.
Resources
Domain
Objective 8 - HCP Terraform
Question 4Skipped

You are deploying virtual machines across multiple cloud regions. Since images are assigned a unique ID per region, you need to create a variable that looks up the correct ID based on the region name. Which code snippet uses the variable type that is most appropriate for this use case?
Correct answer

    variable "image" {
      type = map(string)
      ...
    }

Explanation

Using a map(string) variable type is the most appropriate choice for this use case because it allows you to store key-value pairs, where the key represents the region name and the value represents the corresponding image ID. This way, you can easily look up the correct image ID for a given region when deploying virtual machines across multiple regions.

    variable "image" {
      type = object({ region = string, image_id = string })
      ...
    }

Explanation

Using an object({ region = string, image_id = string }) variable type is not the most appropriate choice for this use case because it defines a specific structure with fixed keys (region and image_id). While this structure could work, it would be less flexible compared to using a map(string) variable type, which allows for dynamic key-value pairs based on the region name and corresponding image ID.

    variable "image" {
      type = list(string)
      ...
    }

Explanation

Using a list(string) variable type is not the most appropriate choice for this use case because it only allows you to store a list of values without associated keys. In this scenario, you need to associate each image ID with a specific region name, which cannot be done with a list variable.

    variable "image" {
      type = set(string)
      ...
    }

Explanation

Using a set(string) variable type is not the most appropriate choice for this use case because sets do not allow duplicate values. Since you need to store different image IDs for each region, a set would not be suitable, as it would not allow multiple image IDs to be associated with different region names.
Overall explanation

Objective 4d - Understand and use complex types

A map(string) is the ideal type for storing key-value pairs where you need to look up values by their keys. In this case, you would use region names as keys (like us-east-1, us-west-2) and AMI IDs as values. You can then reference the correct image for each region using var.image[var.region]. A list would require numeric indexing and wouldn't provide meaningful key-based lookups. A set doesn't support key-value relationships. An object would be overly complex for this simple key-value mapping and would require a predefined structure.
Resources
Domain
Objective 4 - Terraform Configuration
Question 5Skipped

You deployed resources using the CLI, but want to start managing them with Terraform moving forward. What steps are required to start managing resources with Terraform without impacting the resources themselves? (select three)
Correct selection

add import blocks mapping each address to its real ID
Explanation

Adding import blocks mapping each address to its real ID is essential to link the existing resources to their corresponding Terraform configuration. This step allows Terraform to recognize and manage the resources without recreating them.

run terraform apply to recreate all resources from Terraform
Explanation

Running terraform apply to recreate all resources from Terraform is not the correct approach in this scenario. The goal is to start managing existing resources with Terraform without impacting them, not to recreate them from scratch.
Correct selection

write Terraform resource blocks that match the existing settings
Explanation

Writing Terraform resource blocks that match the existing settings is necessary to define the resources in Terraform configuration. This step ensures that Terraform can manage the resources without changing their current settings.

define data sources for all of the existing resources
Explanation

Defining data sources for all of the existing resources is not necessary in this case. Data sources are used to fetch information from external sources, not to manage existing resources that are already deployed.
Correct selection

run terraform apply to import them in state with no changes
Explanation

Running terraform apply to import the resources in state with no changes is crucial to bring the existing resources under Terraform's management without modifying their configurations. This step ensures that Terraform can effectively track and manage resources.
Overall explanation

Objective 7a - Import existing infrastructure into your Terraform workspace

To adopt existing infrastructure, you must have matching resource blocks in code and use imports (import blocks or terraform import) to map real objects into Terraform state. This records them without recreating or modifying the resources; afterward, Terraform manages drift and future changes.
Resources
Domain
Objective 7 - Maintain Infrastructure with Terraform
Question 6Skipped

You added resources from a new provider to an existing configuration, and the next terraform plan returns an error about that provider. What should you do first?

Run terraform validate, which downloads the new plugin and verifies the configuration.
Explanation

This answer is incorrect because validate checks syntax and consistency; it does not install provider plugins.

Create a separate module for the new provider's resources because a single configuration cannot use two providers.
Explanation

This answer is incorrect because a single configuration can use multiple providers; a module is not required.
Correct answer

Run terraform init so Terraform downloads the plugin for the newly added provider.
Explanation

When a new provider is added to the configuration, Terraform needs to download the necessary plugin for that provider. Running terraform init is the first step to initialize the working directory and download the required plugins, including the new provider plugin.

Add the provider to required_providers only, since that alone installs the plugin without terraform init.
Explanation

This answer is incorrect because declaring the provider is necessary but does not download the plugin; terraform init performs the installation.
Overall explanation

Objective 2a - Install and version Terraform providers

In this scenario, you have introduced a new provider. Therefore, Terraform needs to download the plugin to support the new resource it has added. Running terraform init will download the new plugin. Once that is complete, a plan and apply can be executed as needed.

You would need to rerun terraform init after modifying your code for the following reasons:


    Adding a new provider: If you've added a new provider to your code, you'll need to run terraform init to download the provider's binary and configure it.


    Updating the provider configuration: If you've updated the configuration of an existing provider, you'll need to run terraform init to apply the changes.


    Updating the version of a provider: If you've updated the version of a provider, you'll need to run terraform init to download the updated version of the provider's binary.


    Adding or removing a module: If you've added or removed a module from your code, you'll need to run terraform init to download the required modules and dependencies.

In short, terraform init is used to initialize a Terraform working directory, and you'll need to rerun it whenever you make changes to your code that affect the providers, modules, or versions you're using.
Resources
Domain
Objective 2 - Terraform Fundamentals
Question 7Skipped

What is the purpose of Terraform’s dependency graph?

lets providers decide ordering, relying on cloud APIs to handle dependencies
Explanation
Terraform does not let providers decide ordering or rely solely on cloud APIs to handle dependencies. The dependency graph is used by Terraform to determine the correct order of operations based on resource dependencies specified in the configuration.
Correct answer

builds a graph of dependencies to perform create/update/destroy operations from it
Explanation
Terraform's dependency graph is used to build a graph of dependencies between resources in the configuration. This graph is then used to determine the correct order of create, update, and destroy operations to ensure that resources are managed correctly.

sorts resources by type and name and builds a visual graph from them for create operations
Explanation
Terraform does not sort resources by type and name to build a visual graph for create operations. The dependency graph is used for managing dependencies between resources, not for visual representation.

executes resources in the order .tf files appear, ensuring predictable runs
Explanation
Terraform does not execute resources in the order that .tf files appear. Instead, it uses the dependency graph to determine the correct order of operations based on resource dependencies.
Overall explanation

Objective 3d - Generate and review an execution plan for Terraform

Terraform builds a dependency graph and uses it to perform operations, such as generate plans and refresh state.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 8Skipped

Your team is discussing why Terraform maintains a state file. Which of the following are valid reasons why Terraform requires state? (select three)

validate provider credentials before applying changes
Explanation
Validating provider credentials before applying changes is not a reason why Terraform requires a state file. Provider credentials are typically configured separately from the state file and are used to authenticate Terraform with the cloud provider APIs.

automatically fix HCL configuration syntax errors
Explanation
Automatically fixing HCL configuration syntax errors is not a reason why Terraform requires a state file. Syntax errors in your configuration files are typically detected during the plan or apply phases and do not rely on the state file.
Correct selection

track metadata such as resource dependencies
Explanation
The state file is necessary to track metadata such as resource dependencies. This information is crucial for Terraform to understand the relationships between different resources and ensure that changes are applied in the correct order.
Correct selection

improve performance by caching resource attributes
Explanation
Terraform uses the state file to improve performance by caching resource attributes. By storing the current state of your infrastructure, Terraform can avoid making unnecessary API calls to retrieve resource information, which helps speed up the execution of Terraform commands.
Correct selection

map real-world resources to your Terraform configuration
Explanation
Terraform requires a state file to map real-world resources to your Terraform configuration. This allows Terraform to track the current state of your infrastructure and compare it to the desired state defined in your configuration.

encrypt sensitive data in your configuration files
Explanation
Encrypting sensitive data in your configuration files is not a reason why Terraform requires a state file. While it is important to protect sensitive information, such as API keys or passwords, Terraform's state file is not used for encryption purposes.
Overall explanation

Objective 2d - Explain how Terraform uses and manages state

Terraform state is fundamental to how Terraform operates. The state file serves three primary purposes: First, it creates a mapping between your configuration code and real-world resources. When you define an AWS EC2 instance in your .tf file, Terraform records in state which actual EC2 instance (with its specific resource ID) corresponds to that configuration block. Without this mapping, Terraform wouldn't know which resources it manages and which were created by other means.

Second, state tracks metadata that isn't visible in your configuration, such as resource dependencies. Terraform uses this metadata to determine the correct order for creating, updating, or destroying resources; ensuring a VPC exists before creating subnets within it, for example.

Third, state improves performance by caching resource attributes. Instead of querying your cloud provider API for every attribute of every resource during each plan operation, Terraform reads this information from state, making operations significantly faster, especially in large infrastructures. Note that state does NOT handle encryption of configuration files, fix syntax errors, or validate credentials - these are separate Terraform functions handled by other mechanisms.
Domain
Objective 2 - Terraform Fundamentals
Question 9Skipped

Which statements accurately describe tradeoffs between local and remote Terraform state? (select two)

Local state is best for multi-user teams because changes are automatically merged by Terraform.
Explanation
Local state does not automatically merge changes made by different team members, which can lead to conflicts and inconsistencies in the infrastructure codebase. It is not the best choice for multi-user teams as manual coordination is required to prevent conflicts.
Correct selection

Remote state centralizes storage with encryption, locking, and access policies for collaboration.
Explanation
Remote state centralizes storage, providing benefits such as encryption, locking, and access policies that enhance collaboration and security. It allows multiple team members to work on the same infrastructure codebase without the risk of conflicts or data loss.
Correct selection

Local state is simple and works offline, but lacks shared access, locking, and org-level controls.
Explanation
Local state is simple and can be used offline, making it convenient for individual developers. However, it lacks features like shared access, locking mechanisms, and organization-level controls, which are crucial for collaboration and managing infrastructure changes in a team setting.

Remote state removes the need to secure state files since they never contain sensitive values.
Explanation

This is wrong because the state may still contain secrets and must still be protected. Remote state helps secure sensitive information by storing state data in a centralized, secure location. While it may help reduce the risk of exposing sensitive values in state files, sensitive data may still exist within them.
Overall explanation

Objective 6c - Configure remote state using the backend block

Local state stores terraform.tfstate on your machine - simple and offline-friendly, but risky for teams: there’s no built-in locking, sharing, or centralized access control, and the file can contain sensitive data.

Remote state (via a backend such as Azure Blob Storage or S3) centralizes storage and adds collaboration features like state locking, encryption-at-rest, and controlled access, improving safety and auditability. The trade-offs are backend setup, network dependencies, and managing permissions - but for multi-user workflows, remote state is the safer default.
Resources
Domain
Objective 6 - Terraform State Management
Question 10Skipped

You have a variable containing subnet CIDR blocks as a list: ["10.0.5.0/24", "10.0.0.0/24", "10.0.2.0/24"]. You need to determine how many subnets are in the list to use. Which function returns the number of elements?
Correct answer

length(var.subnet_cidrs)
Explanation

The length() function in Terraform is used to return the number of elements in a list. In this case, length(var.subnet_cidrs) will return the number of subnet CIDR blocks in the list, which is what is required to determine how many subnets are in the list.

values(var.subnet_cidrs)
Explanation

The values() function in Terraform is used to return a list of values in a map variable, not the number of elements in a list. It is not suitable for determining the number of subnets in the list of subnet CIDR blocks. The length() function in Terraform is used to return the number of elements in a list. In this case, length(var.subnet_cidrs) will return the number of subnet CIDR blocks in the list.

keys(var.subnet_cidrs)
Explanation

The keys() function in Terraform is used to return a list of keys in a map variable, not the number of elements in a list. It is not applicable in this scenario where the goal is to determine the number of subnets in the list. The length() function in Terraform is used to return the number of elements in a list. In this case, length(var.subnet_cidrs) will return the number of subnet CIDR blocks in the list.

contains(var.subnet_cidrs)
Explanation

The contains() function in Terraform is used to check if a value exists in a list or map. It does not return the number of elements in a list, so it is not the correct function to use in this situation. The length() function in Terraform is used to return the number of elements in a list. In this case, length(var.subnet_cidrs) will return the number of subnet CIDR blocks in the list.
Overall explanation

Objective 4e - Write dynamic configuration using expressions and functions

The length() function returns the number of elements in a list, map, or string. When used with a list like var.subnet_cidrs, it returns the count of items, which can then be used with the count meta-argument. This is a common pattern in Terraform when you need to create multiple resources based on the size of a collection.
Resources
Domain
Objective 4 - Terraform Configuration
Question 11Skipped

Which of the following statements is the most accurate about the Terraform language?

Terraform is a mutable, imperative, Infrastructure as Code provisioning language based on HashiCorp Configuration Language, or optionally YAML.
Explanation
This statement is incorrect. Terraform is not a mutable language and it is not imperative. It is an immutable, declarative Infrastructure as Code provisioning language based on HashiCorp Configuration Language (HCL) or JSON. YAML is not the primary language used for Terraform configuration files.

Terraform is an immutable, imperative, Infrastructure as Code configuration management language based on HashiCorp Configuration Language, or optionally JSON.
Explanation
This statement is inaccurate. Terraform is not an imperative language; it is declarative. Users define the desired state of their infrastructure without specifying the exact steps to achieve that state. This allows Terraform to determine the most efficient way to make changes.

Terraform is a mutable, declarative, Infrastructure as Code configuration management language based on HashiCorp Configuration Language, or optionally JSON.
Explanation
This statement is incorrect. Terraform is not a mutable language; it is immutable. It focuses on defining the desired state of infrastructure and making changes to reach that state, rather than directly modifying existing infrastructure.
Correct answer

Terraform is an immutable, declarative Infrastructure as Code language based on HashiCorp Configuration Language or JSON.
Explanation
Terraform is indeed an immutable and declarative Infrastructure as Code provisioning language. It allows users to define the desired state of their infrastructure and Terraform will make the necessary changes to reach that state. The language is based on HashiCorp Configuration Language (HCL) or JSON for configuration files.
Overall explanation

Objective 1a - Explain what IaC is

Terraform is written in HashiCorp Configuration Language (HCL). However, Terraform also supports a JSON-compatible syntax.

Terraform is primarily designed on immutable infrastructure principles

Terraform is also a declarative language: you simply declare the desired state, and Terraform ensures that real-world resources match that state as written. An imperative approach differs, in which the tool uses a step-by-step workflow to achieve the desired state.
Resources
Domain
Objective 1 - Infrastructure as Code (IaC) with Terraform
Question 12Skipped

After creating several new Terraform configurations, you want to quickly format the files without editing each one manually. How can you update all the files at once?

terraform validate
Explanation

The command terraform validate is used to check the syntax and validate the configuration files for any errors. While it is an important step in the Terraform workflow, it does not address the specific need to format all files simultaneously, making it an incorrect choice for updating all files quickly.

terraform init
Explanation

The command terraform init is used to initialize a working directory containing Terraform configuration files. It prepares the directory for other Terraform commands but does not address the specific need to format all files at once. Therefore, it is not the correct choice for quickly updating all files simultaneously.
Correct answer

terraform fmt
Explanation

The command terraform fmt is used to automatically format all Terraform configuration files in the current directory. It helps maintain consistent formatting across all files without the need for manual editing, making it the ideal choice for quickly updating all files at once.

terraform refresh
Explanation

The command terraform refresh is used to reconcile the state Terraform knows about (via its state file) with the real-world infrastructure. It is not related to formatting Terraform configuration files and does not facilitate quick updates to all files at once, making it an incorrect choice for this scenario.
Overall explanation

Objective 3g - Apply formatting and style adjustments to a configuration

Terraform commands that generate Terraform configuration produce configuration files that conform to the style imposed by terraform fmt. Follow this style in your files to ensure consistency.

The canonical format may change in minor ways between Terraform versions, so after upgrading Terraform HashiCorp recommends to proactively run terraform fmt on your modules, along with any other changes you are making to adopt the new version.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 13Skipped

True or False? By default, the terraform destroy command will prompt the user for confirmation before proceeding.
Correct answer

True
Explanation
By default, the terraform destroy command does prompt the user for confirmation before proceeding to ensure that the user is aware of the resources that will be deleted. This is a safety measure to prevent accidental deletion of important infrastructure.

False
Explanation
The statement is incorrect. In reality, the terraform destroy command does prompt the user for confirmation by default. This is to prevent unintended deletion of resources and to give the user a chance to review the actions that will be taken.
Overall explanation

Objective 3f - Destroy Terraform-managed infrastructure

True. By default, Terraform will prompt for confirmation before proceeding with the terraform destroy command. This prompt allows you to verify that you really want to destroy the infrastructure that Terraform is managing before it actually does so.

Terraform destroy will always prompt for confirmation before executing unless passed the -auto-approve flag.


    $ terraform destroy
    Do you really want to destroy all resources?
      Terraform will destroy all your managed infrastructure, as shown above.
      There is no undo. Only 'yes' will be accepted to confirm.
     
      Enter a value: yes

Resources
Domain
Objective 3 - Core Terraform Workflow
Question 14Skipped

Which statements about sensitive data and Terraform state are correct? (select two)
Correct selection

Marking a variable sensitive = true does not prevent it from being written to state.
Explanation

Marking a variable as sensitive = true in Terraform does not prevent it from being written to the state file. It only ensures that the value is redacted in the CLI output and logs, but it will still be stored in the state file.

Sensitive outputs are never stored in state, only displayed at apply time.
Explanation

Sensitive values (variables or outputs) are still written to state; sensitive = true only affects how values are shown in CLI/UI output. It does not keep them out of state.
Correct selection

The state file can contain sensitive values in plaintext by default.
Explanation
The state file can indeed contain sensitive values in plaintext by default, which can pose a security risk if not handled properly. It is important to be cautious about storing sensitive information in the Terraform state file.

Using local state automatically encrypts secrets on disk without extra steps.
Explanation
Using local state in Terraform does not automatically encrypt secrets on disk without additional steps. It is crucial to implement proper encryption mechanisms and security practices to protect sensitive data stored in the state file, regardless of the state backend used.
Overall explanation

Objective 4h - Understand best practices for managing sensitive data, including secrets management with Vault

Terraform’s state is the source of truth that maps configuration to real infrastructure, so it often contains actual values used during provisioning, including credentials, connection strings, and rendered templates. By default, these values are stored in plaintext in the state file (local or remote). Marking variables or outputs as sensitive = true only redacts them from CLI/UI output; it does not keep the values out of state.

Therefore, protecting secrets means protecting state: prefer remote backends with encryption at rest, IAM/RBAC, and state locking; restrict who can read state; avoid surfacing secrets in outputs; and minimize where secrets appear in configuration. In short, assume state may contain secrets and treat it as sensitive data that must be encrypted, access-controlled, and audited.
Resources
Domain
Objective 4 - Terraform Configuration
Question 15Skipped

Aside from traditional code reviews, which Terraform command provides an opportunity for team members to review each other's work before deployment?

terraform output
Explanation

The terraform output command is used to extract the values of output variables from the state file. While this command is useful for retrieving output values, it does not provide an opportunity for team members to review each other's work before deployment, as it does not show the proposed changes like terraform plan does.
Correct answer

terraform plan
Explanation

The terraform plan command generates an execution plan that describes what Terraform will do when you run terraform apply. This provides an opportunity for team members to review the proposed changes before deployment, allowing them to identify and address any potential issues or mistakes before they are applied to the infrastructure.

terraform apply
Explanation

The terraform apply command is used to apply the changes required to reach the desired state of the configuration. While this command is essential for deploying changes, it does not provide an opportunity for team members to review each other's work before deployment, as the changes are immediately applied to the infrastructure.

terraform validate
Explanation

The terraform validate command is used to validate the configuration files for syntax errors and other issues. While this command is important for ensuring the configuration is valid, it does not provide an opportunity for team members to review each other's work before deployment, as it does not show the proposed changes like terraform plan does.
Overall explanation

Objective 3a - Describe the Terraform workflow

For teams collaborating on infrastructure, Terraform's plan output creates an opportunity for team members to review each other's work. This allows the team to ask questions, evaluate risks, and catch mistakes before any potentially harmful changes are made.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 16Skipped

You have configured a workspace in HCP Terraform to use local execution. In this mode, what does HCP Terraform do?

Local execution blocks local usage for HCP Terraform and requires every run to start from the UI, with no local CLI interaction allowed.
Explanation
This choice is incorrect as local execution in HCP Terraform does not block local usage or require every run to start from the UI. Users can still interact with the CLI locally to run plan and apply commands, with the platform handling the state file synchronization.

HCP Terraform runs all plans and applies in its own environment and streams output back to your CLI session automatically.
Explanation
This choice is incorrect because in local execution mode, HCP Terraform does not run plans and applies in its own environment. Instead, it delegates these tasks to be performed locally on the user's machine, with the platform focusing on managing the state file.

HCP Terraform replaces the state file with variables and policies so state is no longer required for applies.
Explanation
This choice is incorrect because in local execution mode, HCP Terraform still relies on the state file to track the infrastructure's state. It does not replace the state file with variables and policies, as the state file is essential for managing the infrastructure's lifecycle.
Correct answer

HCP Terraform only stores and syncs the workspace’s state file, while you run plan and apply locally on your own machine.
Explanation
When using local execution in HCP Terraform, the platform only handles storing and syncing the workspace's state file. This means that you need to run the plan and apply commands locally on your own machine, while HCP Terraform takes care of managing the state file.
Overall explanation

Objective 8d - Configure and use HCP Terraform integration

Many of HCP Terraform's features rely on remote execution and are not available when using local operations. This includes features like Sentinel policy enforcement, cost estimation, and notifications.

You can disable remote operations for any workspace by changing its Execution Mode to Local. This causes the workspace to act only as a remote backend for Terraform state, with all execution occurring on your own workstations or continuous integration workers.
Resources
Domain
Objective 8 - HCP Terraform
Question 17Skipped

Which Terraform command checks modules, attribute names, and value types to ensure the configuration is syntactically valid and internally consistent?
Correct answer

terraform validate
Explanation
The correct choice is terraform validate because this command is specifically designed to check and report errors within modules, attribute names, and value types to ensure they are syntactically valid and internally consistent.

terraform show
Explanation
The choice terraform show is incorrect as this command is used to provide human-readable output from a state or plan file. It does not perform validation checks for errors within modules, attribute names, and value types.

terraform fmt
Explanation
The choice terraform fmt is incorrect as this command is used to rewrite Terraform configuration files to a canonical format and style. It does not check for errors within modules, attribute names, and value types.

terraform format
Explanation
The choice terraform format is incorrect as this command is not a valid Terraform command. The correct command for formatting Terraform configuration files is terraform fmt.
Overall explanation

Objective 3c - Validate a Terraform configuration

The terraform validate command is used to check and report errors within modules, attribute names, and value types to ensure they are syntactically valid and internally consistent. This command performs basic validation of the Terraform configuration files in the current directory, checking for issues such as missing required attributes, invalid attribute values, and incorrect Terraform code structure.

For example, if you run terraform validate and there are syntax errors in your Terraform code, Terraform will display an error message indicating the line number and description of the issue. If no errors are found, the command will return with no output.

It's recommended to run terraform validate before running terraform apply to ensure that your Terraform code is valid and will not produce unexpected results.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 18Skipped

You are using an HCP Terraform workflow that is connected to a VCS repository (VCS-driven workflow) and want to deploy a new resource. Which sequence of steps follows the recommended workflow?

1. Add the new resource block locally and run terraform apply on your laptop

2. Push the updated terraform.tfstate file to the repo

3. Skip review so HCP Terraform doesn’t re-run the plan
Explanation

Adding the new resource block locally and running terraform apply on your laptop, then pushing the updated terraform.tfstate file to the repository, and skipping the review in HCP Terraform can introduce discrepancies between the local environment and the repository, potentially causing issues with version control and collaboration.

1. Write the new resource block in the HCP Terraform UI

2. Manually start a new run with “Queue plan”

3. Approve the apply when it completes
Explanation

Writing the new resource block directly in the HCP Terraform UI and manually starting a new run with "Queue plan" bypasses the version control and review process provided by VCS-driven workflows. This approach may lead to inconsistencies, lack of visibility, and potential errors in the deployment process.

1. Commit the new resource block directly to the main branch

2. Disable policy checks to allow the apply to run automatically

3. Re-enable policies after the resource is created
Explanation

Committing the new resource block directly to the main branch, disabling policy checks to allow automatic apply, and then re-enabling policies after the resource is created, bypasses the recommended review and approval process. This approach can lead to uncontrolled changes, potential conflicts, and lack of visibility into the deployment process.
Correct answer

1. Add the new resource block to the Terraform configuration in the repo

2. Open a pull request with the change

3. After the plan is generated, approve the apply in HCP Terraform
Explanation

This sequence follows the recommended VCS-driven workflow for deploying a new resource using HCP Terraform. By adding the new resource block to the Terraform configuration in the repository, opening a pull request with the change, and then approving the apply after the plan is generated, you ensure that the deployment process is controlled, reviewed, and tracked effectively.
Overall explanation

Objective 8a - Use HCP Terraform to create infrastructure

In an HCP Terraform VCS-driven workflow, your Terraform configuration lives in version control, not in the UI. To add new infrastructure, you update the code (for example, by adding a new resource block), open a pull request, and let HCP Terraform automatically create a run and show you the plan for that change. After review/approval, you authorize the apply in HCP Terraform.

This gives you change history, review controls, policy checks, and auditable applies instead of people running terraform apply on their laptops.
Resources
Domain
Objective 8 - HCP Terraform
Question 19Skipped

You are using a local backend and accidentally delete the terraform.tfstate file for your workspace. What is the most serious consequence?

Terraform will be required to recreate the missing state file by reading values from variables and outputs in the configuration.
Explanation
Recreating the missing state file by reading values from variables and outputs in the configuration is not a feasible solution. The state file contains detailed information about the current state of the resources, and recreating it manually from variables and outputs would be error-prone and may lead to inconsistencies.

Terraform will need to download the last good state file from the provider API and can then continue managing the resources normally.
Explanation

Terraform does not have the capability to download the last good state file from the provider API. Once the terraform.tfstate file is deleted, Terraform loses the state information and cannot automatically retrieve it from the provider API.

Terraform will require you to switch to using a remote backend until a new local state file is created manually.
Explanation
While switching to a remote backend is a recommended best practice for managing state files, it is not a direct consequence of deleting the local terraform.tfstate file. The primary concern is the loss of state information and the potential for creating duplicate resources or inconsistencies in resource management.
Correct answer

Terraform can no longer track the resources it manages, so the next plan or apply might attempt to create duplicate resources.
Explanation

If the terraform.tfstate file is deleted, Terraform loses track of the current state of the managed resources. This can lead to potential conflicts and inconsistencies when running the next plan or apply commands, as Terraform may attempt to recreate resources that already exist or fail to manage resources that have been modified outside of Terraform.
Overall explanation

Objective 6a - Describe the local backend

Terraform’s state file (terraform.tfstate) is how Terraform knows which real cloud resources it created and is responsible for. If you delete or lose that local state file, Terraform has no record of those resources anymore. On the next terraform plan or terraform apply, it will assume the infrastructure does not exist and may try to create new copies, or it may fail to destroy existing resources because it no longer knows they’re there.

In other words, losing state breaks Terraform’s ability to track and safely manage your infrastructure, and recovery usually means importing resources back into state or restoring from backup. Terraform’s documentation explains that state tracks the mapping between your configuration and the actual remote objects, and that Terraform relies on that mapping for planning and updates.
Resources
Domain
Objective 6 - Terraform State Management
Question 20Skipped

Your configuration includes a validation block in a variable, as shown in the exhibit below. A user sets instance_count = 15. When does Terraform report the validation error?


    variable "instance_count" {
      type = number
      
      validation {
        condition     = var.instance_count > 0 && var.instance_count <= 10
        error_message = "Instance count must be between 1 and 10."
      }
    }

The validation passes because 15 is a valid number type as defined in the variable block
Explanation

The validation condition specified in the variable block is var.instance_count > 0 && var.instance_count <= 10, which means that the value of 15 set by the user does not meet this condition. Therefore, Terraform will report a validation error because the value is outside the specified range.
Correct answer

During terraform validate or terraform plan, before attempting to create any resources
Explanation

Terraform will report the validation error during the terraform validate or terraform plan commands, before attempting to create any resources. This is because the validation block is evaluated during the planning phase to ensure that the specified condition is met before any resource creation is initiated.

During terraform init, when the configuration is first loaded
Explanation

The validation error is not reported during terraform init when the configuration is first loaded. The terraform init command initializes a working directory and downloads any necessary plugins, but it does not evaluate the validation block in variables.

During terraform apply, when Terraform attempts to create the instances
Explanation

The validation error will not occur during terraform apply when Terraform attempts to create the instances. The error is also caught during the planning phase, before any actual resource creation is executed.
Overall explanation

Objective 4g - Validate configuration using custom conditions

Variable validation blocks are checked during the validation phase of Terraform's workflow, which occurs during commands like terraform validate and terraform plan. If the condition evaluates to false, Terraform immediately reports the custom error message and stops execution before attempting to create, modify, or destroy any resources. This early validation helps catch configuration errors before any infrastructure changes are made.
Resources
Domain
Objective 4 - Terraform Configuration
Question 21Skipped

What sets Infrastructure as Code (IaC) apart from managing infrastructure directly instead of making raw API calls or executing CLI commands?

Terraform keeps infrastructure logic in version control to ensure operations remain imperative and run step-by-step.
Explanation
Keeping infrastructure logic in version control is not a defining feature of Infrastructure as Code. While version control is important for managing code changes, it does not directly relate to the concept of IaC, which focuses on defining infrastructure through code rather than manual processes.

Terraform guarantees that resources consistently reflect the desired state by automatically updating them as needed.
Explanation
While Terraform does aim to ensure that resources reflect the desired state, this is not the sole factor that sets Infrastructure as Code apart. The automatic updating of resources is a benefit of using Terraform, but it is not the defining characteristic that distinguishes IaC from managing infrastructure directly.
Correct answer

Terraform uses declarative configuration to describe the desired end state and generates a plan of action before applying changes.
Explanation
Terraform sets Infrastructure as Code apart by using declarative configuration, which means describing the desired end state of the infrastructure rather than the step-by-step process to get there. This approach allows Terraform to generate a plan of action before applying any changes, ensuring predictability and control over infrastructure modifications.

Terraform replaces provider APIs with its own protocol, removing the need to authenticate to each cloud.
Explanation
Terraform does not replace provider APIs with its own protocol. Instead, Terraform interacts with provider APIs to manage infrastructure resources across various cloud platforms. This interaction with provider APIs is essential for Terraform to create, update, and delete resources as defined in the configuration.
Overall explanation

Objective 1a - Explain what IaC is

Infrastructure as Code (IaC) tools enable you to manage infrastructure using configuration files instead of a graphical user interface. IaC helps you build, modify, and manage your infrastructure in a safe, consistent, and repeatable manner by defining resource configurations that you can version, reuse, and share. Terraform is HashiCorp's IaC tool, allowing you to define resources and infrastructure in human-readable, declarative configuration files, while managing your infrastructure's lifecycle.
Resources
Domain
Objective 1 - Infrastructure as Code (IaC) with Terraform
Question 22Skipped

Which of the following variable declarations will cause Terraform to return a type error before apply?

    variable "tags" {
      description = "Map of resource tags"
      type        = map(string)
      default     = {
        Environment = "prod"
        Owner       = "platform-team"
      }
    }

Explanation

The variable declaration for tags is correct as the type is specified as a map of strings and the default value is set as a map with string key-value pairs. This will not cause Terraform to return a type error before apply.

    variable "enabled" {
      description = "Decide whether to enable the feature"
      type        = bool
      default     = true
    }

Explanation

The variable declaration for enabled is correct as the type is specified as a boolean and the default value is set as a boolean (true). This will not cause Terraform to return a type error before apply.
Correct answer

    variable "names" {
      description = "List of user names"
      type        = list(string)
      default     = {}
    }

Explanation

The variable declaration for names is incorrect because the type is specified as a list of strings, but the default value is set as an empty map {}. Terraform will return a type error before apply because an empty map does not match the specified type of list(string).

    variable "instance_count" {
      description = "Number of instances to create"
      type        = number
      default     = 3
    }

Explanation

The variable declaration for instance_count is correct as the type is specified as a number and the default value is set as an integer (3). This will not cause Terraform to return a type error before apply.
Overall explanation

Objective 4c - Use variables and outputs

This variable declaration for a type list is incorrect because a list expects square brackets [ ] and not curly braces. All of the others are correct variable declarations.

From the official HashiCorp documentation found here:

Lists/tuples are represented by a pair of square brackets containing a comma-separated sequence of values, like ["a", 15, true].
Resources
Domain
Objective 4 - Terraform Configuration
Question 23Skipped

How do you specify which provider Terraform should install for a configuration?

Run terraform apply first so Terraform can detect and install the correct provider automatically.
Explanation

Running terraform apply first to let Terraform detect and install the correct provider automatically is not the correct approach. Terraform needs to know which provider to install before applying the configuration, and relying on automatic detection may lead to unexpected behavior or errors.
Correct answer

Define the provider in required_providers and add a matching provider block in the configuration.
Explanation

Defining the provider in the required_providers block in the configuration file is the correct way to specify which provider Terraform should install for a configuration. This ensures that Terraform knows which provider to download and use for the resources defined in the configuration.

Download the provider binary and place it in the working directory with your Terraform configuration files.
Explanation
Downloading the provider binary and placing it in the working directory is not the recommended way to specify which provider Terraform should install. Terraform has a built-in mechanism to manage providers, and manually managing provider binaries can lead to compatibility issues and maintenance challenges.

Set the provider name in a variable and pass it with -var when running terraform init.
Explanation

Setting the provider name in a variable and passing it with -var when running terraform init does not specify which provider Terraform should install for a configuration. Variables passed with -var are used for input values, not for defining providers.
Overall explanation

Objective 2a - Install and version Terraform providers

Terraform doesn’t “magically” install a provider on its own - you tell it what provider to use, what version is allowed, and where to get it. You do this in two parts. First, in the top-level terraform block, you declare the provider in required_providers, including its source address (for example, hashicorp/aws) and an optional version constraint. This tells Terraform which provider to install and from where. Then you add a provider block with the same local name (for example, provider "aws" { region = "us-east-1" }) to configure how that provider should connect (region, endpoint, auth, etc.). After that, running terraform init downloads the specified provider into the working directory so it can be used.
Resources
Domain
Objective 2 - Terraform Fundamentals
Question 24Skipped

You're using a module from the Terraform registry for your infrastructure. When defining the configuration, is it necessary to specify a version argument in the module block?

No, Terraform automatically pins to the first version you initialize with and uses it permanently
Explanation
Terraform does not automatically pin to the first version you initialize with and use it permanently. Without specifying a version, Terraform will default to using the latest version of the module, which may result in unexpected changes in your infrastructure. It is important to explicitly define the version to maintain control over your deployments.
Correct answer

No, the version argument is optional, but it is recommended to ensure consistent and reproducible deployments
Explanation

The version argument in the module block is optional, but it is highly recommended to specify a version to ensure consistent and reproducible deployments. Without a version specified, Terraform defaults to the latest version of the module, which may lead to unexpected changes in your infrastructure.

Yes, but only for public registry modules - private registry modules don't support version constraints
Explanation
The statement is incorrect as both public and private registry modules support version constraints in Terraform. While it is essential to specify a version for both types of modules to ensure consistent deployments, private registry modules are not exempt from version constraints.

Yes, the version argument is always required for registry modules, or Terraform will return an error
Explanation

Contrary to the statement, the version argument is not always required for registry modules. While it is recommended to specify a version for consistency, Terraform will not return an error if the version argument is omitted. However, it is best practice to include a version to avoid potential issues with module updates.
Overall explanation

Objective 5d - Manage module versions

While the version argument is not strictly required for registry modules, omitting it means Terraform will select the latest available version during terraform init. This can lead to unexpected changes if a new module version introduces breaking changes or modified behavior. Best practice is to always specify a version constraint (like version = "~> 2.1.0") to ensure your infrastructure deployments are consistent and reproducible across different team members and environments. The version is then recorded in the dependency lock file for additional consistency.
Resources
Domain
Objective 5 - Terraform Modules
Question 25Skipped

You configure two aws providers in the same module, as shown below, and Terraform returns Error: Duplicate provider configuration and says to set an additional argument for alternative configurations.

Which argument must you add to the second provider block so both can be used?


    provider "aws" {
      region = "us-east-1"
    }
     
    provider "aws" {
      region = "ap-south-1"
    }

version
Explanation

The version argument is not relevant for resolving the Duplicate provider configuration error when configuring multiple providers in the same module. The version argument is used to specify the version constraints for provider plugins.

profile
Explanation

The profile argument is not the solution for resolving the Duplicate provider configuration error. The profile argument is used to specify the AWS profile name to use from the shared credentials file when configuring the AWS provider, but it does not address the issue of duplicate provider configurations.
Correct answer

alias
Explanation

The correct argument to add to the second provider block is alias. By providing a unique alias for each provider block, you can differentiate between multiple provider configurations within the same module and avoid the Duplicate provider configuration error.

locals
Explanation

The locals argument is not the correct option to resolve the Duplicate provider configuration error. locals are used to define values that can be reused throughout a module but do not affect provider configurations.
Overall explanation

Objective 2c - Write Terraform configuration using multiple providers

An alias meta-argument is used when using the same provider with different configurations for different resources. This feature allows you to include multiple provider blocks that refer to different configurations. In this example, you would need something like this:


    provider "aws" {
      region  = "us-east-1"
    }
     
    provider "aws" {
      region = "ap-south-1"
      alias  = "mumbai"
    }

When writing Terraform code to deploy resources, the resources that you want to deploy to the mumbai region would need to specify the alias within the resource block. This instructs Terraform to use the configuration specified in that provider block. So in this case, the resource would be deployed to ap-south-1 region and not the us-east-1 region. This configuration is common when using multiple cloud regions or authentication methods.
Resources
Domain
Objective 2 - Terraform Fundamentals
Question 26Skipped

You are reviewing Terraform code from a colleague and discover a .terraform/ directory. What is the purpose of this directory?

The .terraform/ directory holds cached provider credentials and sensitive variables used for authentication.
Explanation

The .terraform/ directory does not hold cached provider credentials or sensitive variables used for authentication. These sensitive credentials and variables should be managed securely, such as using environment variables or a secure credential management system. The purpose of the .terraform/ directory is to manage Terraform's local working data and components.
Correct answer

The .terraform/ directory stores Terraform’s local working data, including installed provider and module plugins and backend metadata.
Explanation

The .terraform/ directory is used to store Terraform's local working data, such as installed provider and module plugins and backend metadata. This directory helps Terraform manage and track the necessary components for executing infrastructure changes.

The .terraform/ directory contains the validated Terraform configuration and plan files used during terraform apply.
Explanation

The .terraform/ directory does not contain validated Terraform configuration or plan files. These files are typically stored in the working directory or in a separate directory specified by the user. The purpose of the .terraform/ directory is different from storing compiled configuration and plan files.

The .terraform/ directory is where Terraform saves local state files and state backups by default.
Explanation

The .terraform/ directory does not serve as the default location for saving local state files (terraform.tfstate) and state backups. These files are typically stored in the working directory or in a specified backend storage location. The .terraform/ directory is primarily used for managing Terraform's local working data.
Overall explanation

Objective 2b - Describe how Terraform uses providers

The .terraform/ directory is Terraform’s local working cache for a given module. When you run terraform init, Terraform downloads provider binaries into .terraform/providers/, fetches modules into .terraform/modules/, and writes backend/registry metadata it needs to run. It’s not your configuration, and it shouldn’t hold credentials or your terraform.tfstate (state lives in the working directory or a remote backend). In short, .terraform/ lets Terraform operate reproducibly by pinning and caching the exact providers/modules your config requires.
Resources
Domain
Objective 2 - Terraform Fundamentals
Question 27Skipped

Your team uses HCP Terraform with a CLI-driven workflow. After making changes to your configuration locally, you run terraform plan. Where does the plan operation execute?

locally on your machine, then the results are uploaded to HCP Terraform for storage
Explanation
The plan operation in HCP Terraform does not execute locally on your machine and then upload the results to the platform for storage. Instead, the plan is executed directly on the HCP Terraform infrastructure to ensure consistency and accuracy in the planning process. Uploading results from a local machine would introduce potential discrepancies and security risks.

on a self-hosted agent that you must configure in your workspace settings
Explanation
HCP Terraform does not require users to configure self-hosted agents for executing plan operations. The platform handles the execution of terraform plan directly on its infrastructure, eliminating the need for additional configuration or setup on self-hosted agents. This ensures a streamlined and efficient workflow for users.

on your local machine with no interaction with HCP Terraform infrastructure
Explanation
In a CLI-driven workflow with HCP Terraform, the plan operation is not executed solely on your local machine without any interaction with the HCP Terraform infrastructure. The platform plays a crucial role in executing the plan operation to provide accurate insights into the planned changes and their potential impact, ensuring a consistent and reliable planning process.
Correct answer

on HCP Terraform infrastructure with results streamed back to your terminal
Explanation
When you run the terraform plan command in a CLI-driven workflow with HCP Terraform, the plan operation is executed on the HCP Terraform infrastructure. The results of the plan are then streamed back to your terminal for review and analysis, allowing you to see the planned changes and potential impacts directly from the HCP Terraform environment.
Overall explanation

Objective 8a - Use HCP Terraform to create infrastructure

In a CLI-driven workflow with HCP Terraform, plan and apply operations execute remotely on HCP Terraform's infrastructure by default, not on your local machine. This is called remote operations. When you run terraform plan or terraform apply, your configuration files are uploaded to HCP Terraform, the operation runs there, and the output is streamed back to your terminal in real-time. This ensures consistent execution environments, enables better collaboration, and supports features such as policy checks and cost estimation.
Resources
Domain
Objective 8 - HCP Terraform
Question 28Skipped

In HCP Terraform, what is the purpose of using a run trigger?
Correct answer

to automatically queue a new run in one workspace after another workspace applies successfully
Explanation
Using a run trigger in HCP Terraform allows for the automation of queuing a new run in one workspace after another workspace applies successfully. This helps in orchestrating the workflow and dependencies between different workspaces, ensuring that changes are applied in the correct order.

to require human approval before applying changes in specific workspaces, such as production environments
Explanation
Requiring human approval before applying changes in specific workspaces, such as production environments, is not the purpose of using a run trigger in HCP Terraform. Run triggers are designed to automate the workflow and dependencies between workspaces, rather than introducing manual approval steps.

to force a workspace to always run from the newest commit of its VCS branch
Explanation
The purpose of a run trigger is not to force a workspace to always run from the newest commit of its VCS branch. While version control integration is important in Terraform workflows, run triggers specifically focus on the automation of run queuing between workspaces.

to provide temporary cloud credentials so a workspace can authenticate with least privilege during runs
Explanation
Providing temporary cloud credentials for authentication purposes is not the purpose of using a run trigger in HCP Terraform. Run triggers are focused on automating the queuing of runs between workspaces, rather than managing authentication credentials.
Overall explanation

Objective 8c - Describe how to organize and use HCP Terraform workspaces and projects

Run triggers connect workspaces so that when a “source” workspace completes a successful apply, HCP Terraform automatically queues a run in the “downstream” workspace. This is commonly used to keep dependent stacks in sync.
Resources
Domain
Objective 8 - HCP Terraform
Question 29Skipped

Your root module calls a database module that outputs the connection string. It then calls a webapp module that needs this connection string. How do you correctly pass the database connection string from the database module to the webapp module?
Correct answer

    module "database" {
      source = "./modules/database"
      name   = "prod-db"
    }
     
    module "webapp" {
      source            = "./modules/webapp"
      db_connection_str = module.database.connection_string
    }

Explanation

This choice correctly uses the output from the database module to pass the connection string to the webapp module. By referencing module.database.connection_string, the webapp module can access the output from the database module, ensuring the correct connection string is passed between the modules.

    module "database" {
      source = "./modules/database"
      name   = "prod-db"
    }
     
    module "webapp" {
      source            = "./modules/webapp"
      db_connection_str = output.database.connection_string
    }

Explanation

This choice incorrectly tries to access the output from the database module using output.database.connection_string, which is not the correct syntax to access an output from a module. The correct syntax is module.database.connection_string, making this choice incorrect.

    module "database" {
      source = "./modules/database"
      name   = "prod-db"
      
      output "connection_string" {
        to = module.webapp
      }
    }
     
    module "webapp" {
      source = "./modules/webapp"
    }

Explanation

This choice incorrectly tries to define an output in the database module that points to the webapp module. Outputs should be defined in the module where the data originates, not in the module that consumes the data. This makes the implementation incorrect.

    module "database" {
      source = "./modules/database"
      name   = "prod-db"
    }
     
    module "webapp" {
      source            = "./modules/webapp"
      db_connection_str = var.database.connection_string
    }

Explanation

This choice attempts to pass the connection string using var.database.connection_string, which is not the correct syntax to access an output from a module. The correct syntax is module.database.connection_string, making this choice incorrect.
Overall explanation

Objective 5c - Use modules in configuration

Module composition involves using the outputs of one module as the inputs to another. After the database module defines an output called connection_string, you reference it in the root module using module.database.connection_string and pass it as an argument to the webapp module. This creates an implicit dependency—Terraform ensures the database module completes before the webapp module starts. The webapp module must declare a variable named db_connection_str to receive this value. This pattern is fundamental to building complex infrastructure from smaller, reusable modules.
Resources
Domain
Objective 5 - Terraform Modules
Question 30Skipped

You are performing a code review of a colleague's Terraform code and see the following code. Where is this module stored?


    module "vault-aws-tgw" {
      source  = "terraform-aws-modules/transit-gateway/aws"
      version = "3.0.3"
      
      client_id      = var.bk_client
      hvn_id         = var.hvn
      route_table_id = var.rtb_id
    }

Correct answer

the Terraform public registry
Explanation

Based on the information provided in the code snippet, this choice is correct. The code specifies a source from terraform-aws-modules/transit-gateway/aws, a typical format for modules stored in the Terraform public registry.

in a local file under a directory named terraform-aws-modules/transit-gateway/aws
Explanation

The code specifies a source from terraform-aws-modules/transit-gateway/aws , but it does not indicate that the module is stored in a local file under a directory named terraform-aws-modules/transit-gateway/aws . This means that the module is found on the official Terraform registry.

a local code repository on your network
Explanation

The code specifies a source from terraform-aws-modules/transit-gateway/aws, which indicates that the module is not stored in a local code repository on your network. It is referencing an external source.

in an HCP Terraform private registry
Explanation

This choice is incorrect. The code does not indicate that the module is stored in an HCP Terraform private registry. It specifies a source from terraform-aws-modules/transit-gateway/aws , which is indicative of the Terraform public registry.
Overall explanation

Objective 5a - Explain how Terraform sources modules

You can use the Terraform Public Registry by referencing the modules you want to use in your Terraform code and including them as part of your configuration.

To reference a module from the Terraform Public Registry, you can use the module block in your Terraform code. For example, if you want to use a VPC module from the registry, you would add the following code to your Terraform configuration:


    module "vpc" {
      source = "terraform-aws-modules/vpc"
      version = "6.5.0"
     
      # Add any required variables and configuration here
    }


The source attribute specifies the module source, which is the repository on the Terraform Public Registry. The version attribute specifies the version of the module you want to use.

You can also pass values for variables in the module by using them within the module block. For example:


    module "vpc" {
      source = "terraform-aws-modules/vpc"
      version = "6.5.0"
     
      name = "my-vpc"
      cidr = "10.0.0.0/16"
      azs  = ["us-west-2a", "us-west-2b", "us-west-2c"]
    }


Once you've specified the module in your Terraform code, you can use it as you would any other resource. For example, you could reference the VPC ID created by the VPC module with the following code:


    output "vpc_id" {
      value = module.vpc.vpc_id
    }

Resources
Domain
Objective 5 - Terraform Modules
Question 31Skipped

Some of your production resources were created manually in the Azure portal. The company requires all production resources to be managed through Terraform. What should you do to bring those existing resources under Terraform management without disrupting them?

resources created outside of Terraform cannot be managed by Terraform
Explanation

It is incorrect to say that resources created outside of Terraform cannot be managed by Terraform. Terraform provides the ability to import existing resources into its state and configuration files using an import block in the configuration file. This allows management of existing resources without manual intervention or recreation.

Run a terraform get to retrieve other resources that are not under Terraform management.
Explanation

Running terraform get retrieves modules from remote repositories to be used in the Terraform configuration. However, it does not bring existing resources under Terraform management. This command is used to download modules, not to manage existing resources created outside of Terraform.

Rewrite the Terraform configuration file to deploy new resources, run a terraform apply, and migrate users to the newly deployed resources. Manually delete the other resources.
Explanation
Rewriting the Terraform configuration file to deploy new resources and manually deleting existing resources is not the recommended approach for bringing existing resources under Terraform management. This method can lead to disruptions in the production environment and may cause downtime or data loss.
Correct answer

Use the import block to import the existing resources under Terraform management.
Explanation

Using the import block allows you to bring existing resources under Terraform management without disrupting them. This method enables you to map the existing resources to Terraform configuration and state files, ensuring that Terraform can manage them effectively without recreating or deleting them.
Overall explanation

Objective 7a - Import existing infrastructure into your Terraform workspace

To import manually created resources into Terraform without affecting the availability of the deployed resources, follow these steps:


    Import the existing resources: You can use the import block to import the existing resources into Terraform.


    Modify the Terraform configuration: Modify the Terraform configuration to reflect the desired state of the resources. This will allow you to manage the resources using Terraform just like any other Terraform-managed resource


    Test the changes: Before applying the changes, you can use the terraform plan command to preview the changes that will be made to the resources. This will allow you to verify that the changes will not negatively impact the availability of the resources.


    Apply the changes: If the changes are correct, you can use the terraform apply command to officially import the resources and pull them under Terraform management

By following these steps, you can start managing manually created resources in Terraform without impacting the availability of the deployed resources.
Resources
Domain
Objective 7 - Maintain Infrastructure with Terraform
Question 32Skipped

You have split a large module into multiple .tf files and rearranged several resource blocks without changing any arguments or references. What impact should this have when running a terraform plan?
Correct answer

No changes. Block order doesn’t affect the plan because Terraform parses all .tf files in a module together during execution.
Explanation

No changes. The order of resource blocks within .tf files or across multiple .tf files does not affect the execution of Terraform plans. Terraform parses and processes all .tf files in a module together, so the rearrangement of blocks should not impact the plan.

Multiple changes. Providers decide the order based on when blocks appear in the configuration, which may change the plan.
Explanation

Incorrect. The order of resource blocks within .tf files or across multiple .tf files does not affect the execution of Terraform plans. Terraform parses and processes all .tf files in a module together, so the rearrangement of blocks should not impact the plan.

Significant changes. Terraform tracks each resource’s location within each .tf file, so moving blocks can trigger replacements.
Explanation

Incorrect. The order of resource blocks within .tf files or across multiple .tf files does not affect the execution of Terraform plans. Terraform parses and processes all .tf files in a module together, so the rearrangement of blocks should not impact the plan.

Minor changes. Terraform plans resources in the order files are read, so moving blocks to different .tf files can slightly reorder actions.
Explanation

Incorrect. The order of resource blocks within .tf files or across multiple .tf files does not affect the execution of Terraform plans. Terraform parses and processes all .tf files in a module together, so the rearrangement of blocks should not impact the plan.
Overall explanation

Objective 3d - Generate and review an execution plan for Terraform

The Terraform language is declarative, describing the intended goal rather than the steps to achieve it. The ordering of blocks and the files they are organized into are generally not significant; Terraform only considers implicit and explicit relationships between resources when determining an order of operations.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 33Skipped

Why is using a single tool like Terraform for multi-cloud deployments more beneficial than using separate tools and workflows for each cloud?

It automatically migrates existing resources provisioned from other tools into Terraform without refactoring.
Explanation
Terraform does not automatically migrate existing resources provisioned from other tools into its configuration without any refactoring. Migrating existing resources to Terraform requires manual intervention to define the resources in Terraform's configuration language and ensure compatibility with the target cloud provider's APIs and services.

It guarantees identical resource types and APIs across all clouds, eliminating any provider-specific differences.
Explanation

While Terraform provides a consistent way to define infrastructure across multiple cloud providers, it does not guarantee identical resource types or APIs. Each cloud provider may offer unique features and services that Terraform abstracts, but there are still provider-specific differences to consider when deploying resources.
Correct answer

It provides a common workflow and reusable modules, enabling consistent CI/CD and policy across clouds.
Explanation

Using a single tool, such as Terraform, for multi-cloud deployments enables a common workflow and reusable modules, simplifying infrastructure management and deployment. It also enables consistent CI/CD practices and policy enforcement across different cloud providers, leading to increased efficiency and standardization.

It eliminates the need for cloud-specific credentials, since a single token works across all providers by default.
Explanation

Terraform does not eliminate the need for cloud-specific credentials, as each cloud provider requires its own authentication tokens or credentials to interact with their APIs. While Terraform provides a unified way to manage infrastructure, it does not eliminate the need for appropriate credentials for each cloud provider.
Overall explanation

Objective 1c - Explain how Terraform manages multi-cloud, hybrid cloud, and service-agnostic workflows

Using a single tool like Terraform across multiple clouds provides a unified, declarative workflow (init/plan/apply), one language (HCL), and reusable modules. This means teams don’t have to learn and maintain different CLIs, SDKs, or approval patterns for each provider. Such consistency reduces mental effort and errors, makes CI/CD, policy enforcement, reviews, and audit logging consistent, and accelerates onboarding. It also promotes shared libraries and testing practices that work everywhere, enhancing reliability. While you still account for provider-specific details, you manage them behind a common interface, gaining portability and scale without a collection of one-off tools.
Resources
Domain
Objective 1 - Infrastructure as Code (IaC) with Terraform
Question 34Skipped

Which code snippet would enable you to query information about existing resources and use that information within your Terraform configuration?

    locals {
      service_name = "forum"
      owner        = "Community Team"
    }

Explanation
This code snippet defines local variables for service name and owner, which can be used for storing values within the configuration. However, it does not demonstrate querying existing resources for use in the configuration.

    resource "aws_instance" "web" {
      ami           = "ami-502abc4e6b1ab"
      instance_type = "m6g.xlarge"
    }

Explanation
This code snippet defines a resource block for creating an AWS instance with a specific AMI and instance type. While creating resources is a core aspect of Terraform configurations, this snippet does not demonstrate querying existing resources for use in the configuration.

    module "deploy-bk-servers" {
      source  = "./modules/app-cluster"
     
      servers = 5
    }

Explanation
This code snippet defines a module block for deploying backend servers, specifying the source and number of servers. While modules are used for organizing and reusing Terraform configurations, this snippet does not demonstrate querying existing resources for use in the configuration.
Correct answer

    data "aws_ami" "btk-app" {
      most_recent = true
      owners      = ["self"]
     
      filter {
        name   = "tag:Owner"
        values = ["btk-platform"]
      }
    }

Explanation
This code snippet defines a data block that queries AWS for the most recent AMI owned by the current account with a specific tag. This data can then be used within the Terraform configuration to reference the queried AMI for resource creation.

    provider "google" {
      project = "acme-app"
      region  = "us-central1"
    }

Explanation
This code snippet defines a provider block for Google Cloud Platform, specifying the project and region. While providers are essential for interacting with cloud platforms, this snippet does not demonstrate querying existing resources for use in the configuration.
Overall explanation

Objective 4a - Use and differentiate resource and data blocks

In Terraform, data blocks are used to retrieve data from external sources, such as APIs or databases, and make that data available to your Terraform configuration. With data blocks, you can use information from external sources to drive your infrastructure-as-code, making it more dynamic and flexible.

For example, you can use a data block to retrieve a list of Amazon Machine Images (AMIs) from AWS, and then use that data to select the appropriate AMI for a virtual machine you are provisioning:


    data "aws_ami" "example" {
      most_recent = true
     
      filter {
        name   = "name"
        values = ["amzn2-ami-hvm-2.0.*-x86_64-gp2"]
      }
     
      filter {
        name   = "virtualization-type"
        values = ["hvm"]
      }
    }
     
    resource "aws_instance" "example" {
      ami           = data.aws_ami.example.id
      instance_type = "t2.micro"
    }

In this example, the data block retrieves the most recent Amazon Linux 2 HVM AMI, and the aws_instance resource uses the selected AMI to create a virtual machine.

Data blocks can be used to retrieve information from a wide range of sources, such as databases, APIs, and cloud providers. This information can then be used to conditionally create, update, or delete resources, making your Terraform configurations more flexible and adaptable to changing requirements.
Resources
Domain
Objective 4 - Terraform Configuration
Question 35Skipped

You added a new module block to an existing Terraform configuration to reuse infrastructure code from a remote source. What do you need to do so that Terraform downloads the module and makes it available in your working directory?
Correct answer

Run terraform init to install the module into the current working directory.
Explanation

Running terraform init is the correct choice to install the module into the current working directory. This command initializes the Terraform working directory and downloads any modules specified in the configuration.

Run terraform plan so Terraform can fetch the module before creating resources.
Explanation

Running terraform plan is not the correct choice for fetching the module before creating resources. The plan command is used to create an execution plan, but it does not download modules.

Add the module to required_providers so Terraform knows to install it.
Explanation

Adding the module to required_providers is not the correct step to make Terraform download and install the module. The required_providers block is used to specify the provider required for the module, not the module itself.

Copy the module into the .terraform/modules/downloads directory so Terraform can find it.
Explanation

Copying the module into the .terraform/modules/downloads directory is not the correct approach for Terraform to find and use the module. Terraform manages module dependencies and downloads them automatically when terraform init is run.
Overall explanation

Objective 5a - Explain how Terraform sources modules

When you add a module block to your configuration, Terraform does not automatically have that code locally. Running terraform init downloads the module’s source code (along with any providers) into the working directory so it can be used by later commands like plan and apply.
Resources
Domain
Objective 5 - Terraform Modules
Question 36Skipped

Your colleague provided the code snippet below and is looking for assistance in identifying the implicit dependency. What is the implicit dependency in this code?


    resource "aws_eip" "public_ip" {
        vpc      = true
        instance = aws_instance.web_server.id
    }
     
    resource "aws_instance" "web_server" {
      ami           = "ami-502b7f631"
      instance_type = "t2.micro"
      depends_on    = [aws_s3_bucket.company_data]
    }

Correct answer

The EC2 instance labeled web_server
Explanation

The implicit dependency in the code is the EC2 instance labeled web_server because the aws_eip resource depends on the aws_instance.web_server.id for its instance attribute.

The AMI used for the EC2 instance
Explanation

The AMI used for the EC2 instance is not an implicit dependency in this code snippet, as the dependency is explicitly defined using the depends_on attribute for the aws_instance resource.

The S3 bucket labeled company_data
Explanation

The S3 bucket is not the implicit dependency here. It is an explicit dependency of aws_instance.web_server via the depends_on argument. The implicit dependency the question asks about is created by referencing aws_instance.web_server.id in the aws_eip.public_ip resource.

The EIP with an id of eip-2757f631
Explanation

The EIP with an id of eip-2757f631 is not an implicit dependency in this code snippet, as the EIP resource is dependent on the EC2 instance, not on the specific EIP id mentioned in the code.
Overall explanation

Objective 4f - Define resource dependencies in configuration

Implicit dependencies are not explicitly declared in the configuration but are automatically detected by Terraform based on resource relationships. Implicit dependencies allow Terraform to automatically determine the correct order for creating, updating, or deleting resources, ensuring that resources are created in the right order and that dependencies are satisfied.

For example, if you have a resource that depends on another resource, Terraform will automatically detect this relationship and create the dependent resource after the resource it depends on has been created. This allows Terraform to manage complex infrastructure deployments in an efficient and predictable way.

The EC2 instance labeled web_server is the implicit dependency as the aws_eip cannot be created until the aws_instance labeled web_server has been provisioned and the id is available.


Note that aws_s3_bucket.company_data is an explicit dependency for the aws_instance.web_server
Resources
Domain
Objective 4 - Terraform Configuration
Question 37Skipped

In Terraform, what does “drift” mean in the context of a workspace’s state?

The configuration files were refactored and now use different argument ordering.
Explanation
Refactoring configuration files with different argument ordering does not relate to the concept of "drift" in Terraform. Drift specifically refers to changes in the actual infrastructure that are not reflected in the Terraform state.
Correct answer

Real infrastructure has changed outside Terraform and no longer matches the desired state.
Explanation
"Drift" in the context of a workspace's state in Terraform refers to the scenario where the actual infrastructure has been modified outside of Terraform, causing it to deviate from the desired state defined in the configuration. This can lead to inconsistencies and potential issues in managing the infrastructure.

The backend was migrated, but the state contents remained exactly the same.
Explanation
Migrating the backend while keeping the state contents the same does not result in "drift" in Terraform. Drift occurs when the actual infrastructure changes outside of Terraform's control, leading to inconsistencies between the desired and actual states.

Provider versions were upgraded but no resources or state values changed at all.
Explanation
Upgrading provider versions without any changes to resources or state values does not constitute "drift" in Terraform. Drift specifically refers to discrepancies between the desired state and the actual state of the infrastructure.
Overall explanation

Objective 6d - Manage resource drift and Terraform state

One challenge when managing infrastructure as code is drift. Drift is the term for when the real-world state of your infrastructure differs from the state defined in your configuration. This can happen for many reasons. Within the context of your configuration, it happens when adding or removing resources or changing resource definitions. External to your configuration, drift occurs when resources are terminated or fail, or when changes are made manually or via other automation tools.
Resources
Domain
Objective 6 - Terraform State Management
Question 38Skipped

In Terraform, how can a dependency on a provider be established? (select three)
Correct selection

Having existing resource instances for that provider recorded in the current state.
Explanation

Having existing resource instances for a provider recorded in the current state establishes a dependency on that provider. Terraform uses the state file to track the resources managed by each provider, ensuring that the configuration is consistent with the existing infrastructure.

Adding the provider’s documentation URL as a comment in the Terraform code.
Explanation
Adding the provider's documentation URL as a comment in the Terraform code does not establish a dependency on the provider. While documentation is important for understanding how to use a provider, it does not directly impact the dependency relationship between the configuration and the provider.

Existence of any provider plugins found locally in the working directory.
Explanation
The existence of provider plugins locally in the working directory does not establish a dependency on a provider. While having the necessary plugins locally is important for Terraform to interact with providers, it is not the primary method for establishing a dependency on a provider in the configuration.
Correct selection

Declaring a provider block in the configuration, or adding required_version constraints.
Explanation

Declaring a provider block in the configuration is essential to establish a dependency on a specific provider. This ensures that the configuration uses the correct provider version to manage resources.
Correct selection

Using a resource or data block that belongs to that provider in the configuration.
Explanation

Using a resource or data block that belongs to a specific provider in the configuration creates a direct dependency on that provider. This indicates that the resources being managed in the configuration require the functionality provided by that specific provider.
Overall explanation

Objective 2b - Describe how Terraform uses providers

Terraform relies on plugins called "providers" to interact with remote systems. Terraform configurations must declare which providers they require, so that Terraform can install and use them.
Resources
Domain
Objective 2 - Terraform Fundamentals
Question 39Skipped

Why should users not commit the terraform.tfstate file to version control? (select two)

Committing the file to version control prevents Terraform from reading it to properly manage state.
Explanation

Committing the terraform.tfstate file to version control does not prevent Terraform from reading it to manage state. In fact, version control is not the recommended method for storing and managing the state file, as it can lead to security vulnerabilities and operational issues.

Storing state in Git prevents you from safely updating infrastructure by reverting a resource change.
Explanation
Storing the state file in Git does not prevent you from safely updating infrastructure by reverting a resource change. In fact, using version control for the state file can introduce complexities and risks that may hinder the ability to manage infrastructure changes effectively.
Correct selection

State can include plaintext secrets and detailed resource data and commit history can expose them.
Explanation

Committing the terraform.tfstate file to version control can expose plaintext secrets and detailed resource data, as well as the commit history. This can pose a security risk by making sensitive information accessible to unauthorized users.
Correct selection

VCS provides no state locking, so concurrent runs can cause conflicting commits and corrupt state.
Explanation
Version control systems (VCS) do not provide state locking, which means that concurrent runs of Terraform can lead to conflicting commits and potentially corrupt the state file. This can result in inconsistencies and errors in the infrastructure management process.
Overall explanation

Objective 6a - Describe the local backend

Do not store terraform.tfstate state files, provider credentials, or sensitive values in version control. Use a .gitignore file to avoid accidentally committing sensitive files.
Resources
Domain
Objective 6 - Terraform State Management
Question 40Skipped

True or False? In the configuration below, the aws_volume_attachment.attach_data resource has an implicit dependency on both the instance and the volume.


    resource "aws_instance" "app_core" {
      ami               = "ami-0c55b159cbfafe1f0"
      instance_type     = "t3.micro"
      availability_zone = "ca-central-1a"
     
      tags = { Owner = "btk-platform", Env = "pr0d-east" }
    }
     
    resource "aws_ebs_volume" "data_pr0d_east" {
      availability_zone = "ca-central-1a"
      size              = 10
    }
     
    resource "aws_volume_attachment" "attach_data" {
      device_name = "/dev/xvdf"
      volume_id   = aws_ebs_volume.data_pr0d_east.id
      instance_id = aws_instance.app_core.id
    }

Correct answer

True
Explanation

Correct. The aws_volume_attachment.attach_data resource has an implicit dependency on both the aws_instance.app_core and aws_ebs_volume.data_pr0d_east resources. This means that Terraform will automatically ensure that the instance and volume are created before attempting to attach the volume to the instance.

False
Explanation

Incorrect. The configuration clearly shows that the aws_volume_attachment.attach_data resource is implicitly dependent on both the aws_instance.app_core and aws_ebs_volume.data_pr0d_east resources. This dependency ensures that the volume attachment occurs only after the instance and volume are successfully created.
Overall explanation

Objective 4f - Define resource dependencies in configuration

Terraform implicit dependencies refer to the connections between resources in a Terraform configuration that are not explicitly defined. Terraform uses a graph to track these implicit dependencies and makes sure that resources are created, updated, and deleted in the right order.

For example, if you have a Terraform configuration that creates a virtual machine and a disk, Terraform will implicitly depend on the disk being created before the virtual machine because it needs the disk to function. Terraform will automatically create the disk first and then the virtual machine.

Sometimes, Terraform may miss an implicit dependency, resulting in an error when you run terraform apply. In these cases, you can use the depends_on argument to explicitly declare the dependency between resources. That would make the following example an explicit dependency. For example:


    resource "aws_instance" "example" {
      ami           = "ami-0c55b159cbfafe502"
      instance_type = "t2.micro"
     
      depends_on = [
        aws_ebs_volume.bk-data
      ]
    }
     
    resource "aws_ebs_volume" "bk-data" {
      availability_zone = "us-west-2a"
      size              = 1
    }

In this example, the aws_instance resource depends on the aws_ebs_volume resource, and Terraform will create the aws_ebs_volume resource first and then the aws_instance resource.

In general, Terraform's implicit dependencies are handled automatically, but sometimes it may be necessary to use the depends_on argument to ensure that resources are created in the correct order.
Resources
Domain
Objective 4 - Terraform Configuration
Question 41Skipped

What are some advantages of using Infrastructure as Code in an organization? (select three)
Correct selection

IaC code can be used to manage infrastructure on multiple cloud platforms.
Explanation
IaC code is platform-agnostic and can be used to manage infrastructure across various cloud platforms, providing flexibility and scalability in managing resources.
Correct selection

IaC allows you to save your configurations in version control, enabling safe collaboration on infrastructure.
Explanation
Using Infrastructure as Code (IaC) allows for configurations to be stored in version control, enabling collaboration, tracking changes, and ensuring consistency in infrastructure deployment.
Correct selection

IaC uses a human-readable configuration language to help you write infrastructure code quickly.
Explanation
IaC utilizes a human-readable configuration language, making it easier for developers and operators to understand, write, and maintain infrastructure code efficiently.

IaC is written as an imperative approach, where specific commands need to be executed in the correct order.
Explanation
This statement is incorrect. IaC follows a declarative approach, where the desired state of the infrastructure is defined, rather than specific commands to be executed in a particular order.
Overall explanation

Objective 1b - Describe the advantages of IaC patterns

Infrastructure as Code has many benefits. For starters, IaC allows you to create a blueprint of your data center as code that can be versioned, shared, and reused. Because IaC is code, it can (and should) be stored and managed in a code repository, such as GitHub, GitLab, or Bitbucket.  Changes can be proposed or submitted via Pull Requests (PRs), which can help ensure a proper workflow, enable an approval process, and follow a typical development lifecycle.

One of the primary reasons that Terraform (or other IaC tools) is becoming more popular is that they are mostly platform agnostic. You can use Terraform to provision and manage resources on various platforms, SaaS products, and even local infrastructure.

IaC is generally easy to read (and develop). Terraform is written in HashiCorp Configuration Language (HCL), while others may use YAML or solution-specific languages (like Microsoft ARM). But generally, IaC code is easy to read and understand
Resources
Domain
Objective 1 - Infrastructure as Code (IaC) with Terraform
Question 42Skipped

You're deploying AWS infrastructure and writing the configuration as shown below. What does the reference aws_vpc.main.id in the subnet configuration accomplish?


    resource "aws_vpc" "main" {
      cidr_block = "10.5.0.0/16"
      
      tags = {
        Name = "production-vpc"
      }
    }
     
    resource "aws_subnet" "public" {
      vpc_id     = aws_vpc.main.id
      cidr_block = "10.5.2.0/24"
      
      tags = {
        Name = "bk-public-subnet"
      }
    }

it copies the VPC's CIDR block to use in the subnet configuration
Explanation

This choice is incorrect because referencing aws_vpc.main.id does not copy the VPC's CIDR block to use in the subnet configuration. The CIDR block for the subnet is explicitly defined in the subnet configuration and is not automatically inherited from the VPC.

it validates that the VPC name matches the subnet's requirements
Explanation

This choice is incorrect because referencing aws_vpc.main.id does not validate that the VPC name matches the subnet's requirements. The reference is used to establish the relationship between the VPC and the subnet based on their IDs, not their names.

it creates a data source that queries AWS for the VPC information
Explanation

This choice is incorrect because referencing aws_vpc.main.id does not create a data source that queries AWS for the VPC information. Instead, it directly accesses the VPC's ID from the previously defined aws_vpc resource.
Correct answer

it retrieves the VPC's ID and creates an implicit dependency
Explanation

The reference aws_vpc.main.id in the subnet configuration retrieves the VPC's ID and creates an implicit dependency between the VPC and the subnet. This ensures that the subnet is associated with the correct VPC and can only be created after the VPC has been successfully provisioned.
Overall explanation

Objective 4b - Refer to resource attributes and create cross-resource references

When you reference another resource's attribute using the syntax resource_type.resource_name.attribute, Terraform both retrieves that attribute's value and automatically creates a dependency between the resources. In this case, aws_vpc.main.id tells Terraform to use the VPC's ID for the subnet's vpc_id argument and ensures the VPC must be created before attempting to create the subnet. This implicit dependency is how Terraform determines the correct order for resource creation.
Resources
Domain
Objective 4 - Terraform Configuration
Question 43Skipped

The security team requires that you protect sensitive input values, such as API keys or passwords, in Terraform. Which methods follow Terraform guidance for reducing accidental exposure of secrets? (select two)
Correct selection

Provide secrets as short-lived, ephemeral values from an external system (for example, Vault) at runtime instead of hardcoding static credentials in version-controlled Terraform files.
Explanation

Providing secrets as short-lived, ephemeral values from an external system like Vault at runtime instead of hardcoding them in version-controlled Terraform files helps reduce the exposure of credentials. This approach follows Terraform best practices for securely managing sensitive information.

Expose secret values with an output block so they appear after apply and can be copied from run logs for future troubleshooting.
Explanation

Exposing secret values with an output block after apply and allowing them to appear in run logs for future troubleshooting increases the risk of accidental exposure of credentials. This practice does not align with Terraform's recommendations for protecting sensitive input values and can compromise security.

Store credentials in a terraform.tfvars and commit to Git so all team members can reuse the same values without needing separate secure access.
Explanation

Storing credentials in a terraform.tfvars file and committing it to Git may expose sensitive information to unauthorized users. This practice goes against Terraform guidance for reducing accidental exposure of secrets and can lead to security vulnerabilities.
Correct selection

Mark variables as sensitive so Terraform redacts their values in CLI output and logs, limiting accidental disclosure of credentials during plan and apply.
Explanation

By marking variables as sensitive in Terraform, their values will be redacted in CLI output and logs, reducing the risk of accidental exposure of credentials during plan and apply operations. This aligns with Terraform's guidance on protecting sensitive input values.
Overall explanation

Objective 4h - Understand best practices for managing sensitive data, including secrets management with Vault

Terraform can mark variables as sensitive, which redacts their values in CLI output and logs, helping avoid accidental leaks.

Terraform also recommends supplying secrets at runtime from external systems like Vault instead of hardcoding them, so credentials can be short-lived and not committed to source. This implies using ephemeral values rather than storing long-lived secrets in code
Resources
Domain
Objective 4 - Terraform Configuration
Question 44Skipped

A colleague asks what the difference is between terraform show and terraform state show. What is the correct explanation? (select three)
Correct selection

terraform show displays the entire state file without requiring any additional arguments
Explanation

terraform show is a command that displays the entire state file, including all resources and their current configuration, without the need for any additional arguments. It provides a high-level overview of the managed infrastructure.

terraform show only displays resources that have pending changes
Explanation

terraform show does not only display resources that have pending changes. It shows the current state of all resources in the Terraform configuration, regardless of whether there are pending changes.
Correct selection

terraform show is useful when you want a complete overview of all managed infrastructure
Explanation

terraform show is particularly useful when you want to have a comprehensive overview of all the managed infrastructure in your Terraform configuration. It allows you to see the current state of all resources at once.

terraform state show automatically formats its output as JSON for machine parsing
Explanation

terraform state show does not automatically format its output as JSON for machine parsing. The output format is human-readable and provides detailed information about the state of a specific resource within the Terraform state.
Correct selection

terraform state show requires you to specify a resource address to view that specific resource
Explanation

When using terraform state show, you need to specify a resource address to view the details of that specific resource. This command allows you to inspect the state of individual resources within the Terraform state.

terraform state show can display multiple resources by separating them with commas
Explanation

terraform state show does not have the capability to display multiple resources by separating them with commas. It is designed to show the state of a single resource specified by its address.
Overall explanation

Objective 7b - Use the CLI to inspect state

The key differences between these commands relate to scope and usage. The terraform show command displays the complete contents of the state file in human-readable format without requiring any arguments, making it ideal for getting a full overview of your infrastructure. In contrast, terraform state show is targeted and requires a specific resource address as an argument, such as terraform state show aws_instance.web, to view details about just that resource.
Resources
Domain
Objective 7 - Maintain Infrastructure with Terraform
Question 45Skipped

You've decided to change your backend configuration from S3 to HCP Terraform. After updating the backend block, you run terraform init. Which flag should you use to reconfigure the backend without copying the existing state?
Correct answer

terraform init -reconfigure
Explanation

Using the -reconfigure flag with the terraform init command allows you to reconfigure the backend without copying the existing state. This is the correct flag to use when changing the backend configuration without transferring the existing state data.

terraform init -migrate-state
Explanation

The -migrate-state flag is not the correct option in this scenario. This flag is used for migrating state data from one backend to another, rather than simply reconfiguring the backend without copying the existing state.

terraform init -force-copy
Explanation

This answer is incorrect because -force-copy does the opposite of what is required. It is a real terraform init flag, but it suppresses the migration prompt and answers yes, forcing Terraform to copy (migrate) the existing state into the new backend. Since the goal is to reconfigure without copying state, -reconfigure is the correct flag."

terraform init -backend=false
Explanation

The -backend=false flag is not the appropriate choice for reconfiguring the backend without copying the existing state. This flag disables the backend configuration entirely, which is not the desired outcome in this situation.
Overall explanation

Objective 6c - Configure remote state using the backend block

The -reconfigure flag tells Terraform to reconfigure the backend without attempting to migrate existing state. This is useful when you want to initialize a new backend configuration without copying the old state - perhaps because you've already manually migrated it or because you're setting up a fresh configuration.
Resources
Domain
Objective 6 - Terraform State Management
Question 46Skipped

You are calling a child module named network from your root module. In your root module, you attempt to reference the VPC ID as displayed below. When you run terraform plan, Terraform returns an error that vpc_id is not a valid attribute for module.network.

What is the most likely cause of this error?


    resource "aws_subnet" "primary_core" {
      vpc_id     = module.network.vpc_id
      cidr_block = "10.5.0.0/23"
    }

The root module must use a data block to read the VPC ID instead of referencing module values directly.
Explanation

While using a data block to read the VPC ID is a valid approach, the error in this case is more likely due to the absence of an output block in the network module. This absence prevents the root module from directly referencing the VPC ID, leading to the error message.
Correct answer

The network module did not define an output block that exports the VPC ID, resulting in the error.
Explanation

The error is occurring because the network module did not define an output block that exports the VPC ID. Without an output block, the root module cannot access the VPC ID from the child module, resulting in the error message.

Module outputs can only be referenced during apply, not during plan, so the reference is premature.
Explanation

Module outputs can indeed be referenced during the plan phase, so the issue is not related to the timing of the reference. The error is more likely due to the missing output block in the network module, which prevents the root module from accessing the VPC ID.

You must declare vpc_id as a variable in the root module before you can read it from a child module.
Explanation

Declaring vpc_id as a variable in the root module is not necessary for reading it from a child module. The error is more likely caused by the absence of an output block in the network module, which is needed to export the VPC ID for use in the root module.
Overall explanation

Objective 5b - Describe variable scope within modules

The resources defined in a module form a self-contained group of resources. Module authors commonly define outputs that expose the attribute values of resources created by the module. You can reference the values in the parent module using the module.<MODULE-NAME>.<OUTPUT-NAME> expression
Resources
Domain
Objective 5 - Terraform Modules
Question 47Skipped

You have a Terraform project with multiple subdirectories: /dev, /staging, and /prod. Each directory contains a separate Terraform configuration for each different environment. Before deploying resources, where do you need to run terraform init?

Only in the root directory to initialize all environments at once.
Explanation

Running terraform init only in the root directory to initialize all environments at once is not the correct approach. Each environment (/dev, /staging, and /prod) has its own configuration and dependencies, so it is necessary to run terraform init in each directory to ensure that the correct resources are provisioned for each environment.

Only in /prod since production requires initialization, but dev and staging can share the configuration.
Explanation

While production may require initialization before deployment, it is essential to run terraform init in each directory (/dev, /staging, and /prod) because each environment has its own configuration and dependencies. Sharing the configuration between dev and staging without proper initialization can lead to unexpected behavior and deployment issues.
Correct answer

In each directory (/dev, /staging, and /prod) since each is a separate working directory.
Explanation

Running terraform init in each directory is necessary because each directory contains a separate Terraform configuration for a different environment. This command initializes the working directory and prepares it for Terraform operations, ensuring that the necessary plugins and modules are available for that specific environment.

In any one of the directories, then copy the .terraform folder to the other directories.
Explanation

While it is possible to run terraform init in one directory and then copy the .terraform folder to other directories, this approach is not recommended. Each environment may have different dependencies, modules, and configurations, so running terraform init in each directory ensures that the correct resources are initialized and provisioned for that specific environment.
Overall explanation

Objective 3b - Initialize a Terraform working directory

Each directory containing Terraform configuration files is a separate working directory and must be initialized independently. Running terraform init in one directory only initializes that specific directory - it downloads providers and modules to a .terraform subdirectory within that directory.

Even though /dev, /staging, and /prod might use the same providers, each needs its own initialization because each has its own local .terraform directory for downloaded plugins. You must navigate to each directory and run terraform init before you can plan or apply changes in that environment.

Never copy the .terraform directory between working directories; always run init in each location.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 48Skipped

In a Terraform module block that sources a module from a registry, why should you include the version argument?

to set the required Terraform CLI version for the workspace
Explanation

Setting the required Terraform CLI version for the workspace is not the purpose of including the version argument in a Terraform module block. The version argument specifically pertains to pinning a specific module release and does not control the Terraform CLI version.

to lock down the version of Terraform that can be used with the module
Explanation

Including the version argument in a Terraform module block does not lock down the version of Terraform that can be used with the module. It specifically pertains to pinning a specific module release and does not restrict the version of Terraform that can interact with the module.

to lock provider versions used inside the child module
Explanation

The version argument in a Terraform module block is used to specify the version of the module itself, not to lock provider versions used inside the child module. Provider versions are typically managed separately in the provider configuration.
Correct answer

to pin a specific module release and avoid unexpected upgrades
Explanation

Including the version argument in a Terraform module block allows you to specify a specific version of the module to use. This helps to ensure that the module does not unexpectedly upgrade to a newer version, which could potentially introduce breaking changes or compatibility issues with the existing infrastructure.
Overall explanation

Objective 5d - Manage module versions

When source points to a registry module, a version (or version constraint) tells Terraform which module release to install, providing reproducible runs and preventing accidental upgrades. This argument applies only to registry sources; it does not set the Terraform CLI version, does not lock provider versions inside the child module, and does not change how code is downloaded.
Resources
Domain
Objective 5 - Terraform Modules
Question 49Skipped

Your team's change management process requires that all Terraform changes be reviewed and approved before execution. You run terraform plan -out=bk-project.tfplan and send the output for review. After approval is granted two hours later, what command should you run to execute the exact changes that were reviewed?

run terraform apply -auto-approve to skip the confirmation prompt
Explanation

Using terraform apply -auto-approve to skip the confirmation prompt may bypass the review and approval process mandated by the change management policy. It is important to execute the exact changes that were reviewed and approved to maintain compliance with the established procedures using the terraform apply bk-project.tfplan command.
Correct answer

run terraform apply bk-project.tfplan to execute the saved plan
Explanation

Running terraform apply bk-project.tfplan will execute the exact changes that were reviewed and approved in the saved plan file. This ensures that only approved changes are applied to the infrastructure, in accordance with the change management process requirements.

run terraform plan again to verify no drift occurred, then run terraform apply
Explanation

Running terraform plan again after approval and then applying the changes with terraform apply is redundant and may introduce unnecessary delays in the execution process. It is more efficient to directly apply the approved plan using the terraform apply bk-project.tfplan command.

run terraform apply to apply the latest configuration changes since Terraform automatically executes the last plan
Explanation

Running terraform apply without specifying a plan file will not execute the exact changes that were reviewed and approved. Terraform will automatically generate a new plan based on the latest configuration, potentially including changes that were not reviewed and approved. It is important to execute the exact changes that were reviewed and approved to maintain compliance with the established procedures using the terraform apply bk-project.tfplan command.
Overall explanation

Objective 3e - Apply changes to infrastructure with Terraform

When you need to ensure that the exact changes reviewed are applied without any drift or modifications, you use the saved plan file with terraform apply bk-project.tfplan. This command applies the specific plan that was generated and reviewed, preventing any surprises from configuration changes, state drift, or other modifications that might have occurred between planning and execution.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 50Skipped

In the following Terraform code, what do  name, cidr, and azs represent, and what purpose do they serve?


    module "vpc" {
      source  = "terraform-aws-modules/vpc/aws"
      version = "5.0.2"
     
      name               = var.vpc_name            
      cidr               = var.vpc_cidr_block          
      azs                = var.vpc_azs             
      tags               = merge(var.vpc_tags, {
        Owner       = "btk-platform"
        Environment = "pr0d-east"
      })
    }

the value of these variables will be obtained from values created within the child module
Explanation
The values of the variables like name, cidr, and azs are not obtained from values created within the child module. Instead, these variables are typically defined and assigned values in the calling module or through input variables provided during the Terraform execution.
Correct answer

these are module-specific inputs that are passed into the child module used for resource creation
Explanation

The declarations like name, cidr, and azs are module-specific inputs that are being passed into the child module for resource creation. These allow for customization and flexibility in configuring the VPC module according to specific requirements.

these are the outputs that the child module will return
Explanation
The declarations in the Terraform code are not outputs that the child module will return. Outputs are used to expose certain values to other parts of the Terraform configuration, whereas these declarations are used as input variables.

these are where the variable declarations are created so Terraform is aware of these variables within the calling module
Explanation
The declarations in the code are not where variable declarations are created. They are actually instances where the variables are being assigned values passed from the calling module. Variable declarations are typically defined in separate variable files or within the same module.
Overall explanation

Objective 5b - Describe variable scope within modules

To pass values to a Terraform module when calling the module in your code, you use input variables. Input variables are a way to pass values into a Terraform module from the calling code. They make the module flexible and reusable, as the same module can be used with different input values in different contexts.

In this example, the values for the name, cidr, and azs inputs are passed to the module as values of variables. The variables are defined in the calling code in the calling module using the variable block.

To pass the values to the module, you can specify them in a number of ways, such as:

    Using command-line flags when running Terraform

    Storing the values in a Terraform .tfvars file and passing that file to Terraform when running it

    Using environment variables

Resources
Domain
Objective 5 - Terraform Modules
Question 51Skipped

When assigning a value to a Terraform input variable through an environment variable, which prefix string is necessary?

TF_ENV_VAR_name
Explanation

The prefix TF_ENV_VAR_ is not the correct prefix string to use when assigning a value to a Terraform input variable through an environment variable. The correct prefix for this purpose is TF_VAR_ to ensure that the variable is properly recognized and utilized by Terraform.
Correct answer

TF_VAR_
Explanation

The prefix string TF_VAR_ is necessary when assigning a value to a Terraform input variable through an environment variable. This prefix indicates that the environment variable is intended to set a Terraform variable.

TF_ENV_
Explanation

The prefix TF_ENV_ is not the correct prefix string to use when assigning a value to a Terraform input variable through an environment variable. The correct prefix for this purpose is TF_VAR_ to ensure that the variable is properly recognized and utilized by Terraform.

TF_VAR_VALUE
Explanation

The prefix TF_VAR_VALUE is not the correct prefix string to use when assigning a value to a Terraform input variable through an environment variable. The correct prefix for this purpose is TF_VAR_ to ensure that the variable is properly recognized and utilized by Terraform.
Overall explanation

Objective 4c - Use variables and outputs

Terraform allows you to use environment variables to set values in your Terraform configuration. This can be useful for specifying values specific to the environment in which Terraform is running or providing values that can be easily changed without modifying the Terraform configuration.

To use a variable in Terraform, you need to define the variable using the following syntax in your Terraform configuration:


    variable "instructor_name" {
      type = string
    }

You can then set the value of the environment variable when you run Terraform by exporting the variable in your shell before running any Terraform commands:


    $ export TF_VAR_instructor_name="bryan"
    $ terraform apply


https://developer.hashicorp.com/terraform/cli/config/environment-variables
Resources
Domain
Objective 4 - Terraform Configuration
Question 52Skipped

Why is state locking necessary when using a remote backend?
Correct answer

prevents concurrent runs from writing to the same state at the same time to avoid state corruption.
Explanation
State locking is necessary when using a remote backend to prevent concurrent runs from writing to the same state at the same time. This helps avoid conflicts and potential state corruption that can occur when multiple instances of Terraform try to modify the state simultaneously.

encrypts the state file at rest and in transit during all Terraform operations to ensure the security of sensitive values
Explanation
This choice is incorrect because state locking is not primarily used for encrypting the state file. While encryption is important for security, state locking's main purpose is to prevent concurrent writes and ensure the integrity of the state file during Terraform operations.

make plans and applies faster by caching state data on the backend server for Terraform to access
Explanation
This choice is incorrect because state locking is not related to caching state data on the backend server. State locking is specifically used to prevent concurrent writes to the state file, not for caching purposes.

automatically roll back partial changes if an apply fails on any targeted resource to improve cost savings
Explanation
This choice is incorrect because state locking does not automatically roll back partial changes if an apply fails on any targeted resource. State locking is focused on preventing concurrent writes to the state file, not on managing apply failures or cost savings.
Overall explanation

Objective 6b - Describe state locking

If supported by your backend, Terraform will lock your state for all operations that could write state. This prevents others from acquiring the lock and potentially corrupting your state.

State locking happens automatically on all operations that could write state. You do not see any message that it happens. If state locking fails, Terraform does not continue.
Resources
Domain
Objective 6 - Terraform State Management
Question 53Skipped

A resource was changed manually outside of Terraform. You don't want to make any changes yet, but you want to see how the state would be updated to match the current real-world values. Which command should you run?

terraform apply -refresh-only
Explanation

terraform apply -refresh-only updates the state file (and can write changes), which the question explicitly wants to avoid. We only want to see what would change, so plan -refresh-only is the better choice.

terraform state pull
Explanation

Running terraform state pull will retrieve the current state data from the Terraform state file, but it will not show you how the state would be updated to match the current real-world values after a manual change outside of Terraform.

terraform validate
Explanation

Running terraform validate will check the configuration files for syntax errors and other issues, but it will not show you how the state would be updated to match the current real-world values after a manual change outside of Terraform.
Correct answer

terraform plan -refresh-only
Explanation

Running terraform plan -refresh-only will show you how the state would be updated to match the current real-world values without making any changes to the infrastructure. This command will refresh the state of the resources without applying any changes.
Overall explanation

Objective 6d - Manage resource drift and Terraform state

Refresh-only mode: creates a plan whose goal is only to update the Terraform state and any root module output values to match changes made to remote objects outside of Terraform. This can be useful if you've intentionally changed one or more remote objects outside of the usual workflow (e.g., while responding to an incident) and you now need to reconcile Terraform's records with those changes.

Activate refresh-only mode using the -refresh-only command line option.
Resources
Domain
Objective 6 - Terraform State Management
Question 54Skipped

Which statement best describes a Terraform data source?

a reusable package that defines multiple resources and outputs to be instantiated by other configurations
Explanation

This choice describes a Terraform module, not a data source. A module is a reusable package that can contain multiple resources, variables, and outputs, which can be instantiated by other configurations to promote reusability and modularity in Terraform code.

a mechanism for persisting variable defaults into state so they can be reused by other workspaces
Explanation

This choice describes a mechanism for persisting variable defaults into state, which is not related to Terraform data sources. Data sources are used to fetch information from external APIs, not to store variable defaults for reuse in other workspaces.

a local cache that stores computed values between runs to speed up terraform apply
Explanation

This choice describes a local cache, which is not the purpose of a Terraform data source. Data sources do not store computed values between runs; instead, they dynamically fetch information from external sources at runtime to populate Terraform configurations.
Correct answer

a read-only construct that queries provider APIs and returns attributes for use elsewhere in the configuration
Explanation

A Terraform data source is a read-only construct that allows querying provider APIs to retrieve specific attributes or information that can be used in other parts of the configuration. It helps in integrating external data into Terraform configurations without actually creating or managing any infrastructure resources.
Overall explanation

Objective 4a - Use and differentiate `resource` and `data` blocks

Data sources enable fetching or computing data for use elsewhere in a Terraform configuration. The use of data sources enables a Terraform configuration to leverage information defined outside of Terraform or by another separate Terraform configuration.
Resources
Domain
Objective 4 - Terraform Configuration
Question 55Skipped

You run terraform init in a new working directory. The output shows Terraform downloading the aws and time provider plugins. On this machine, where does Terraform store those provider plugins?
Correct answer

The .terraform/providers directory in the current working directory
Explanation

The provider plugins downloaded by Terraform during the initialization process are stored in the .terraform/providers directory within the current working directory. This directory contains all the necessary provider plugins for the Terraform configuration to interact with the specified providers.

The .terraform/plugins directory in the current working directory
Explanation

The .terraform/plugins directory in the current working directory is not the correct location where Terraform stores the downloaded provider plugins. The provider plugins are stored in the .terraform/providers directory, which is the standard location for managing provider plugins in a Terraform project.

The .terraform.d directory in the current working directory
Explanation

The .terraform.d directory in the current working directory is not the correct location where Terraform stores the downloaded provider plugins. The provider plugins are specifically stored in the .terraform/providers directory for easy access and management within the Terraform project.

/etc/terraform/plugins
Explanation

The /etc/terraform/plugins directory is not the correct location where Terraform stores the downloaded provider plugins. Terraform stores the provider plugins in the .terraform/providers directory within the current working directory to ensure that the plugins are easily accessible and managed within the project structure.
Overall explanation

Objective 2a - Install and version Terraform providers

By default, terraform init downloads plugins into a subdirectory of the working directory, .terraform/providers so that each working directory is self-contained.


Resources
Domain
Objective 2 - Terraform Fundamentals
Question 56Skipped

Your organization manages a Google Cloud project with 50 resources across multiple services. You need to decommission only the Cloud SQL database instance and its backup policy while keeping all other infrastructure running. What is the most appropriate approach?

Run terraform destroy to remove all resources, then run terraform apply to recreate everything except the database
Explanation

Running terraform destroy will remove all resources, including those you want to keep. Recreating everything except the database by running terraform apply afterwards is not an efficient approach and does not align with the requirement to only decommission the Cloud SQL database instance and its backup policy while keeping the rest of the infrastructure running.
Correct answer

Remove the database and backup resource blocks from your configuration, then run terraform apply.
Explanation

By removing the database and backup resource blocks from your configuration and then running terraform apply, you specifically target only the resources you want to decommission while keeping the rest of the infrastructure intact. This approach ensures that only the Cloud SQL database instance and its backup policy are removed, meeting the requirement to decommission those resources while preserving the others.

Run terraform destroy -target=google_sql_database_instance.main -target=google_sql_backup_run.policy
Explanation

Using terraform destroy with specific targets for the Google Cloud SQL database instance and backup policy may remove those resources, but it is a risky approach as it can have unintended consequences on dependencies or related resources. It is safer and more precise to directly remove the resource blocks from your configuration and then apply the changes.

Add two removed blocks to your configuration, pointing to each of the Cloud SQL database and the backup policy
Explanation

Adding removed blocks pointing to the Cloud SQL database and backup policy in your configuration does not align with the requirement to decommission only those specific resources while keeping the rest of the infrastructure running. This approach may lead to unnecessary changes and potential disruptions to the existing infrastructure. The most appropriate approach is to remove the resource blocks and apply the changes.
Overall explanation

Objective 3f - Destroy Terraform-managed infrastructure

The config-driven approach of removing resource blocks from your configuration and running terraform apply is the recommended method for selectively removing resources. When you delete the resource blocks and apply, Terraform will see that those resources are no longer part of the desired state and will destroy them while preserving all other infrastructure. This approach is declarative, version-controlled, and follows Terraform best practices. The changes are tracked in your configuration files, making it clear to your team what infrastructure is being removed.
Resources
Domain
Objective 3 - Core Terraform Workflow
Question 57Skipped

Your team is building a reusable Terraform module for web servers. The module must always create exactly two instances, and you want Terraform to fail if a caller tries to use any other value during plan or apply.

Which approach should you use to enforce this requirement?

Use the count meta-argument in resource blocks to force the creation of two instances regardless of the variable value.
Explanation

Using the count meta-argument in resource blocks to force the creation of two instances regardless of the variable value will indeed create two instances. However, this approach does not provide validation or enforce the requirement during the plan and apply phase, so it may not guarantee that exactly two instances are deployed as needed.

Rely on Terraform’s number type constraint and document the requirement in the README
Explanation

Relying on Terraform's number type constraint and documenting the requirement in the README is a good practice for informing users about the expected input. However, this approach does not enforce the validation during the plan and apply phase, so it may not guarantee the deployment of exactly two instances.
Correct answer

Add a validation block that checks the variable equals 2 and provides an error message if it does not.
Explanation

Adding a validation block that checks the variable equals 2 and provides an error message if it does not, ensuring that Terraform will validate the input during the plan and apply phase, guaranteeing that exactly two instances are deployed as required for the web servers.

Set the variable’s default to 2 within the variables.tf file
Explanation

Setting the variable's default to 2 within the variables.tf file will provide a default value if the user does not specify one. However, this approach does not enforce the requirement of deploying exactly two instances during the plan and apply phase, as the user could still override the default value.
Overall explanation

Objective 4g - Validate configuration using custom conditions

The validation block with condition = var.instance_count == 2 enforces that only the value 2 is accepted, and emits a clear error message otherwise. This leverages Terraform’s input variable validation for uniquely restrictive requirements.


    variable "instance_count" {
      type = number
     
      validation {
        condition     = var.instance_count == 2
        error_message = "You must request two web instances."
      }
    }

Resources
Domain
Objective 4 - Terraform Configuration
HashiCorp Certified: Terraform Associate (004)
Set a certification due date
HashiCorp Certified: Terraform Associate (004)

    Completed

Start
Practice Test 1: Terraform Associate 004 - Exam #1
Not completed
Start
Practice Test 2: Terraform Associate 004 - Exam #2
Not completed
Start
Practice Test 3: Terraform Associate 004 - Exam #3
Not completed
Start
Practice Test 4: Terraform Associate 004 - Exam #4
Not completed
Start
Practice Test 5: Terraform Associate 004 - Exam #5
Not completed

    Start
    Practice Test 6: Terraform Associate 004 - Exam #6

information alert
Schedule learning time
Learning a little each day adds up. Research shows that students who make learning a habit are more likely to reach their goals. Set time aside to learn and get reminders using your learning scheduler.
Prepare for your certification with this course.
HashiCorp Certified: Terraform Associate (004)
HashiCorp Certified: Terraform Associate (004)
Terraform Associate 004 Certification Practice Exams - Almost 350 Realistic Questions with Detailed Explanations + Links
Rating: 4.7 out of 54.7
811 ratings
9,021
Students
342 questions
Total
Last updated July 2026
English
What you'll learn

    The most UP-TO-DATE course available - constantly updating questions based on student feedback from real exam results
    Brand new and updated practice exams questions for Terraform Associate 004
    Almost 350 questions across SIX UNIQUE practice questions with detailed explanations and links for the HashiCorp Certified: Terraform Associate
    Learn how to pass the HashiCorp Certified: Terraform Associate 004 certification exam on your first try
    Understanding of Terraform, its components, and how to identify what areas you need to focus on before taking the exam
    How to be comfortable answering complex questions to prepare you for the exam
    Built by an AUTHORIZED HASHICORP INSTRUCTOR for Consul, Vault, and Terraform

Description

Looking for Terraform Associate 004 practice exams that feel like the real test? This course delivers almost 350 expertly crafted questions, mapped one-to-one to the Terraform Associate (TA-004) objectives, including Terraform 1.12 and HCP Terraform topics. You’ll identify gaps fast, learn the “why” behind each answer, and build the confidence to pass.

What makes my practice tests different?

    Objective-aligned: Every item is tied to TA-004 domains (IaC, fundamentals, workflow, configuration, modules, state, maintenance/import, and HCP Terraform).

    Real-exam feel: Scenario-based stems, multi-select, and tricky distractors that mirror the latest style and difficulty.

    Deep explanations: Clear rationales for correct and incorrect options, plus pointers to official docs, so you study smarter.

    Continuously refined: Updated as Terraform and the TA-004 guidance evolve.

    Ethical & Compliant: No exam dumps. Questions are 100% original, and I always create content thinking from the student's perspective.

What’s Inside the Course:

    Almost 350 practice questions across six full-length tests

    Coverage of Terraform 1.12 features and HCP Terraform (workspaces, projects, variable sets, policy enforcement, drift detection, and more)

    Objective review mode to target weak areas (providers & lockfiles, state backends/locking, import & moved/removed blocks, variables/outputs/functions, modules & versioning, plan/apply/destroy, fmt/validate/checks)

About Your Instructor:
I’m Bryan - a HashiCorp Ambassador, Authorized Terraform Trainer, and instructor with 200k+ learners. I specialize in simplifying complex tools with real-world examples and hands-on labs. My practice exams are known for accuracy, clarity, and helpful explanations that teach - not just test.

Why learners choose this course:

    Proven results: Trusted by thousands preparing for Terraform Associate

    Exam-focused: Format, structure, and difficulty modeled on TA-004

    Learning-first design: You’ll understand concepts instead of memorizing trivia

Enroll now to practice with confidence and walk into the Terraform Associate 004 exam ready to pass.

What you’ll learn:

    Practice with 300+ Terraform Associate 004 questions that mirror real exam style

    Master Terraform 1.12 fundamentals: providers, dependency lock file, CLI workflow, plan/apply/destroy, fmt/validate

    Use state effectively: local vs remote backends, locking, drift detection, refresh-only, moved/removed blocks

    Work with configuration & language: resources vs data sources, references, variables/outputs, complex types, functions, validations/checks

    Build with modules: sourcing, versioning, composition, variable scope, outputs

    Import existing infrastructure and safely manage state changes

    Apply HCP Terraform features: workspaces & projects, variable sets, runs & remote ops, policy enforcement, drift detection, teams & governance

    Recognize security & sensitive data best practices (Vault provider and sensitive input handling)

    Strengthen exam strategy with realistic multi-select and scenario items

    Identify weaknesses quickly with objective-based review modes

Who this course is for:

    Engineers, architects, and operators preparing for the HashiCorp Certified: Terraform Associate 004 exam

    Learners who want ethical, realistic practice tests with thorough explanations

    Anyone upgrading knowledge to Terraform 1.12 and HCP Terraform concepts covered in TA-004

Course includes:

    300+ original practice questions (multiple-choice, multi-select, true/false)

    Detailed explanations and official-docs references

    Objective-focused full-length mock exams

    Continuous updates - no dumps, no shortcuts

FAQs:

Q: Is this course aligned to Terraform Associate 004 (TA-004)?
A: Yes. The questions and coverage map directly to the TA-004 objectives, including Terraform 1.12 and HCP Terraform topics.

Q: What are the Key Differences Between the Terraform 003 and 004 Exams?
A: The Key Differences include four new topics, Tests on Terraform version 1.12, and includes new HCP Terraform content

Q: Does this include HCP Terraform (workspaces, projects, policies, drift)?
A: Yes - HCP Terraform collaboration, governance, variable sets, projects, run triggers, policy enforcement, and drift detection are all covered.

Q: Are these exam dumps?
A: No. These are original, teaching-oriented questions with explanations and links to official references.

Q: Will this course help if I used the older Terraform 003 materials?
A: Yes. TA-004 adjusts emphasis and includes Terraform 1.12 & HCP Terraform features. These tests were built from the ground up to reflect the newer blueprint and language updates.

Q: How often is the course updated?
A: Regularly. Items are refined as Terraform and TA-004 guidance evolve and as I receive student feedback.
Who this course is for:

    Students aiming to pass the HashiCorp Certified: Terraform Associate certification
    DevOps Engineers looking to automate infrastructure deployment

Instructor
Bryan Krausen - 250,000+ Enrollments Worldwide
Premier Udemy Instructor | Udemy Instructor Partner

Bryan Krausen is a Udemy Instructor Partner, Udemy Community Champion, and top-rated Udemy Instructor. He is also a HashiCorp Ambassador and AWS Community Builder and has been involved with the IT community for 20+ years.

Bryan Krausen's career spans over 25 years in enterprise IT and includes data centers, networking, security, cloud, and consulting. Bryan was the principal architect at two leading consultancies, a testament to his deep understanding of the field. His expertise lies in cloud technologies, where he excels in designing, implementing, and optimizing client solutions. He leverages platforms like Amazon Web Services (AWS) and a suite of industry-leading tools, including HashiCorp Vault, Terraform, Consul, Nomad, and Packer, to deliver top-notch results. Additional skills include deployment and integrations with GitHub, Kubernetes, Nomad, and much more.

Beyond consulting, he shares his knowledge through publications and speaking engagements, having authored three (3) books and delivered training to over 150,000 students. Bryan often shares insights at community events, including HashiConf, HashiTalks, HashiCorp User Groups, and other major tech conferences.

All course material can be found on GitHub.
Requirements

    Strong understanding on Terraform and how to write Infrastructure as Code
    Desire to take and pass the HashiCorp Certified: Terraform Associate certification
    Going through the official HashiCorp exam review

    Get the app
    About us

    Help and Support
    Terms

    Privacy policy

    Sitemap
    Accessibility statement

© 2026 Udemy, Inc.
